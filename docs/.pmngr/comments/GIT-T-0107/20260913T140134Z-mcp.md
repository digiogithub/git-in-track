---
type: comment
item: GIT-T-0107
author: mcp
created: 2026-09-13T14:01:34Z
kind: comment
---

Core half landed; `internal/vault/sprint.go` and `internal/server/sprints.go` are outside the files I was assigned, so the wiring is left for their owner.

Ready to call:

- `core.ParseSprintStatus(string) (SprintStatus, error)` — decodes one value of a `?status=current,upcoming` query string (case-insensitive, trimmed).
- `core.FilterSprintsByStatus(sprints, now, statuses)` — empty filter keeps everything.
- `core.SortSprintsForListing(sprints, now)` — sorts in place: current → upcoming → draft → completed, ties broken by start date then id. `SprintStatusOrder()` and `SprintStatus.Rank()` expose the order if a different surface needs it.
- `core.SprintSummary.Status` is already populated by `SummarizeSprint`, so every payload built from a summary carries the derived status with no further work.

All three take `now` as a parameter — `internal/core` reads no clock (ADR-034). The caller localises to the team timezone.
