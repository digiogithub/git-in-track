---
type: comment
item: GIT-T-0194
author: mcp
created: 2026-09-15T16:43:59Z
kind: comment
---

Closing-pass verification, all four criteria met: in `tools/list` on a read-only server with schemas inferred from the structs -> internal/mcp/tools_search.go:62-75 `register(toolDef{Name:"search_semantic", Untrusted:true})` with no `Write`, called from `registerSearchTools` via tools.go:24-32, pinned by internal/mcp/tools_test.go:15-19 and asserted by TestToolSurface (both subtests pass); untrusted note appended -> tools.go:54-57 `untrustedNote` added by `register`, asserted by TestUntrustedContentIsMarked/search_semantic_description; unavailable backend returns a structured tool error -> tools_search.go:133-153 `semanticFailure` rewriting `codeUnavailable` with the `search_items`/`search_kb` retry; in-memory harness behaviour test -> TestSearchSemantic (6 subtests: hit with rev/status/snippet and forwarded args, empty result, limit default+cap, kind scoping, unavailable backend, invalid query/kind) — `go test -race ./internal/mcp/` passes. No business logic in the package: the handler only validates (invalidField), dispatches `search.semantic` and projects through wire.Hit.
