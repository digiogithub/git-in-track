---
type: comment
item: GIT-US-0044
author: mcp
created: 2026-09-13T14:04:04Z
kind: comment
---

Landed. `core.External{System, ID, URL, Key, SyncedAt}` plus `ExternalRef` (the normalised
`(system, id)` identity) in `model.go`; set algebra, normalisation and `validateExternal` in the new
`internal/core/external.go`; read/written by `ParseItem`/`ParseComment`/`SerializeItem`/`SerializeComment`
and by `ParsePage`; `ItemDraft.External` and `ItemPatch.External`/`AddExternal`/`RemoveExternal` with
merge-on-re-add semantics; `Index.byExternal` with `ItemByExternal`/`ItemIDByExternal`/`ExternalRefs`
and `Filter.ExternalSystem`/`ExternalID`. Docs: `03-data-model.md` §3.2, §7.1/§10.1/§11.2, new §12.5
(R-EXT-1..7) and §18; ADR-031 accepted and indexed.

Two things a reviewer must know. (1) On `KBPage` the field is `ExternalRefs`, not `External`:
`KBPage.External` was already the list of outbound URLs scanned out of the body, and renaming it
would have touched `snapshot.go` and the web app. The front-matter key is still `external`.
(2) The `CHANGELOG.md` entry required by GIT-T-0005 was **not** written — `CHANGELOG.md` is outside
the file set this wave allowed me to touch; the suggested line is in that task's notes.
Verified with `go build ./...`, `go vet`, `go test -race ./internal/core/...`, `gofmt -l` and a
`GOOS=js GOARCH=wasm` build, all clean.
