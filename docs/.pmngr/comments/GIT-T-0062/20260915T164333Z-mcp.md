---
type: comment
item: GIT-T-0062
author: mcp
created: 2026-09-15T16:43:33Z
kind: comment
---

Closing-pass verification (read-only review of HEAD 62f03ec). Status left at `in_review`: 3 of 4 criteria met.

Met: three-column shell collapsing without horizontal overflow -> `ui/AgentPage.tsx:209-224` (list `hidden`/`block` below `lg`), `:226-232` (conversation hides when the list is open below `lg`), `:325-328` (rail `hidden … xl:block`); thread list survives a reload, newest first, derived title -> `ui/threadMeta.ts:33` (`gintrack:agent:threads`, per-repo, try/catch-guarded) + `ui/model.ts` `mergeThreadRows`, tests `AgentPage.test.tsx:140` and `model.test.ts`; vitest covers persistence, title derivation and deletion-from-storage -> `AgentPage.test.tsx:140,165`, `model.test.ts`.

**Not met:** "Deleting a thread removes it from storage and selects a neighbour." The storage half is done (`AgentPage.tsx:200-203`, `forgetThreadMeta`), but the selection half is contradicted at **`web/src/features/agent/store.ts:532`**: `if (get().threadId === threadId) await get().newThread();` — deleting the *active* conversation drops the user into a brand-new empty thread even when other conversations exist, instead of selecting the neighbouring one. No test asserts the post-delete selection either (`AgentPage.test.tsx:165` asserts only `agentDeletes` and the forgotten title).

Smallest fix: in `store.ts:521-533`, capture the row list before filtering and, when the deleted id was active, `await get().selectThread(next.id)` for the neighbour (the row after the deleted one, else the one before) and fall back to `newThread()` only when the filtered list is empty; add one assertion to `AgentPage.test.tsx:165` that `threadId` becomes the surviving thread's id.
