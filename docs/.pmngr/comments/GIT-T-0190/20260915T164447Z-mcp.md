---
type: comment
item: GIT-T-0190
author: mcp
created: 2026-09-15T16:44:47Z
kind: comment
---

Closing review, all criteria verified: the toggle hides and restores the section and persists -> web/src/app/ui-prefs.ts:36 (`semanticResults`), :66 (default `true`), :94 (read back), :123 (written to the persisted slice under `gintrack:ui-prefs` at :10), :151 (`setSemanticResults`), wired at WorkspaceSearch.tsx:170-172 and :217-229; test "hides the semantic section when the toggle is off and remembers it" checks the localStorage payload and a fresh mount, plus src/app/ui-prefs.test.ts "persists the semantic results toggle across a reload" and "keeps the other layout settings when the toggle is written"; absent without the capability -> WorkspaceSearch.tsx:217 renders the `Switch` only when `semanticSupported`, asserted by `queryByRole('switch')` in "leaves no semantic section behind when the capability is not pando". `npx vitest run src/app/ui-prefs.test.ts src/features/workspace/WorkspaceSearch.test.tsx` passes.
