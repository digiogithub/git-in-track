---
type: comment
item: GIT-T-0197
author: mcp
created: 2026-09-13T15:40:46Z
kind: comment
---

Left `todo`: half done, and the other half is in files owned by other agents this wave.

Done: `docs/08-mcp-server.md` §4.17 documents `push_comment_to_youtrack` with an input/output example, and says the three things a reader has to know — `pushed` means *queued*, a comment already carrying a reference is `skipped` rather than sent twice, and an item with no YouTrack reference is refused non-retryably. It also states that `push_comments: auto` makes the tool unnecessary, because every `add_comment` is queued by the same seam in the vault's `comment.add`.

Still open, in `docs/07-cli-and-api.md` §4 and `CHANGELOG.md`: `gintrack youtrack push-comments <item>` with `--all`, `--comment`, `--wait` and `--json`. The command itself is unwritten (GIT-T-0195).
