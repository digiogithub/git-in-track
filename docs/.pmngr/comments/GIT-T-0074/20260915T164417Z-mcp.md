---
type: comment
item: GIT-T-0074
author: mcp
created: 2026-09-15T16:44:17Z
kind: comment
---

Closing-pass verification (read-only review of HEAD 62f03ec). All four criteria met: offered choices render as checkboxes (single-select stays single) and a question with none falls back to a text field, with free text always available alongside as Pando's `otherText` -> `ui/QuestionDialog.tsx:66-75,118-160`, tests `QuestionDialog.test.tsx:44,58,68,78`; submitting delivers the answer and the run continues -> `ui/InterruptSlot.tsx:118-123` calling `buildQuestionAnswer` (`hitl.ts:186-208`, SDK `answerQuestion`, `questionId` = index plus the human `header`) through `store.ts:437` `resume`, test `hitl.test.ts:112`; cancelling delivers an explicit cancelled result rather than silence -> `InterruptSlot.tsx:124` -> `hitl.ts:161-166` `refusalFor` -> SDK `cancelQuestion()`, which lands in the transcript as that tool call's result (`ui/ToolCallCard.tsx`), test `QuestionDialog.test.tsx:83,94`; Escape confirms first so a stray keypress cannot end the turn -> `QuestionDialog.tsx:168-181`. Suite green (17 files / 162 tests).
