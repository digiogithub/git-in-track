---
type: comment
item: GIT-T-0185
author: mcp
created: 2026-09-15T16:44:23Z
kind: comment
---

Closing review, all criteria verified: two labelled groups in the right order -> web/src/features/workspace/WorkspaceSearch.tsx:236-259 ("Exact matches" list, then "Related by meaning"), rows with title, relevance and snippet at :131-148; the semantic group is absent entirely without the capability -> WorkspaceSearch.tsx:169-182 (`semanticSupported`/`showSemantic` gate the filter and the whole block, no heading rendered when the list is empty), test "leaves no semantic section behind when the capability is not pando" asserts neither the list, the heading text, nor the toggle exists; debounce and the two-character minimum untouched -> :165-167 (`useDeferredValue`, `MIN_QUERY = 2`) with test "does not query on a single character"; ordering and both capability states covered -> `npx vitest run src/features/workspace/WorkspaceSearch.test.tsx` (10 tests pass; "groups semantic hits below the exact ones" asserts document order via `compareDocumentPosition`). No command palette or new primitive introduced.
