---
type: comment
item: GIT-US-0086
author: mcp
created: 2026-09-15T16:45:04Z
kind: comment
---

Closed: all four child tasks (GIT-T-0181, GIT-T-0185, GIT-T-0189, GIT-T-0190) verified `done`. Evidence per criterion: distinct groups, exact first -> WorkspaceSearch.tsx:236-259 + test "groups semantic hits below the exact ones" (document order asserted); snippet, relevance and highlighting -> :131-148, :26-72, `relevanceLabel` :79-83, test "highlights the query terms the snippet contains"; section absent without `'pando'`, no empty heading -> :169-182, test "leaves no semantic section behind when the capability is not pando"; toggle hides/restores and survives a reload -> ui-prefs.ts:36,66,94,123,151 + tests in src/app/ui-prefs.test.ts and "hides the semantic section when the toggle is off and remembers it"; `SearchHit` carries origin and optional snippet, all providers compile -> provider.ts:1345-1375, companion-provider.ts:963-999, browser-provider.ts:664-671, fake-provider.ts:1885-1918, `npx tsc --noEmit` clean; snippet escaped, raw HTML not executed -> text nodes only, no `dangerouslySetInnerHTML` in the file, test "shows HTML inside a snippet as text"; long snippets clamped with an accessible expand -> :95-127 (`aria-expanded` button), test "clamps a long snippet behind an expand control"; Vitest -> `npx vitest run src/features/workspace/WorkspaceSearch.test.tsx src/api/search-provider.test.ts src/app/ui-prefs.test.ts src/api/companion-provider.test.ts src/api/browser-provider.test.ts` = 5 files, 158 tests, all passing. The docs/05-web-app.md patch the implementation comment left to the coordinator has landed (docs/05-web-app.md:188 and :794).
