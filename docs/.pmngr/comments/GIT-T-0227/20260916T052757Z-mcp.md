---
type: comment
item: GIT-T-0227
author: mcp
created: 2026-09-16T05:27:57Z
kind: comment
---

Both amendments implemented and verified. Working tree, uncommitted.

`[AGUI]` zero values: new `isZeroTOMLValue` (`''`, `0`, `0.0`, `false`, `[]`, `{}` after normalisation) applied only when the table is `AGUI`; the key's line span is rewritten in place so the user's ordering survives, and the template's comment rides along only when the existing key carries none, so a note the user wrote is never displaced. Against a copy of the real 453-line config, divergences drop from 16 to 7, `[AGUI]` is filled in, `AutoApprove` and `AllowedOrigins` produce neither a write nor a divergence, the user's `AgentPoolSize`/`AgentPoolTTL` are left alone, and a second run is byte-identical.

Skip-and-continue: the pre-check builds a `skipped` list instead of failing, the write loop skips those targets, the report names them and points at `--force`, exit is `exitOK`, and `--json` gained `skipped`. Stale "re-run with `--force`" advice refreshed in `pando.toml.tmpl` (3 places), the no-token stdout paragraph and three `docs/20` troubleshooting rows. CHANGELOG flags the breaking change for scripts that used exit 5 to detect an already-configured repository.

`exitConflict` keeps five callers in this command — the unparsable table header (`agent_merge.go:508`) and four in the `pando secret` path (`agent.go:452, 461, 475, 481`) — so nothing became dead code. Confirmed by grep, not by report.

Tests: `TestAgentInitRefusesToOverwrite` replaced by `TestAgentInitSkipsTheFilesItWouldOverwrite`, keeping the byte-unchanged assertion and adding `exitOK`, the stdout names and a changed `--companion-url` landing in the merged file. New `TestAgentInitMergeFillsInAGUIZeroValues` pins both halves of the scoped rule: `[AGUI]` zero values filled, `[MCPServer] StdioEnabled` and `[PersonaAutoSelect] PersonaPath` still diverging. `TestAgentInitMergeKeepsEverythingItDoesNotOwn` now states the guarantee as "may only add, and fill in inside `[AGUI]`".

Verified independently of the implementer's report: `go build ./...` clean, `go test -race ./cmd/gintrack/...` ok, `gofmt -l cmd` empty, `make lint` 0 issues, and `/www/git-in-track/.pando.toml` unmodified.

One residue for a later decision, not a blocker: `[PersonaAutoSelect] PersonaPath = ''` is an empty string outside `[AGUI]`, so it warns rather than taking `'./agents/personas'`. Harmless today because `PersonaAutoSelect.Enabled` is false on both sides.
