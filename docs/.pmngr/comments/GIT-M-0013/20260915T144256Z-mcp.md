---
type: comment
item: GIT-M-0013
author: mcp
created: 2026-09-15T14:42:56Z
kind: comment
---

**Pando dependencies lifted (2026-09-15).** Pando delivered the whole PANDO backlog on 2026-09-14: PANDO-M-0001 and PANDO-M-0002 done, PANDO-EP-0001..0007 done, 29 of 29 stories, commits `5fde84a3..c539b603` (21). Verified in code: `[AGUI] Tools`/`Mesnada` and `[AGUI.Profiles.<name>]` in `internal/config/config.go`; `GET {path}/healthz`, `/threads`, `/threads/{id}/messages`, `/threads/{id}/stream`, `DELETE /threads/{id}`, `POST /runs/{id}/cancel` in `internal/agui/server.go`; `POST /api/v1/remembrances/kb/{documents,reindex,search}` and `/code/search` in `internal/api/routes.go`; `HttpToken`/`HttpAllowedOrigins` on the MCP transport; `agui-serve --token-file` and `PANDO_AGUI_TOKEN`; SDK with `sideEffects: false`, `browser` conditions, `./agui/client` entry, `thread.ts` and `hitl.ts`. Installed binary `pando v0.705.1-0.20260914211356-c539b6039b76` carries all of it.

Still open on the Pando side, none blocking: PANDO-T-0001 (SDK browser-build smoke test in CI), PANDO-T-0004 (regenerate non-HITL SSE fixtures), PANDO-T-0005 (test data race). One open decision before GIT-US-0053 starts: `@pando-ai/sdk` is not on npmjs, so `web/` needs a distribution channel (publish, git dependency or workspace link).

Every story in this milestone is buildable now.
