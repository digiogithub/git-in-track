---
type: comment
item: GIT-T-0013
author: mcp
created: 2026-09-15T16:43:22Z
kind: comment
---

Closing review, all 4 criteria verified. Frame-by-frame delivery -> `TestAgentRunStreamsFrameByFrame` (internal/server/agent_test.go:313) genuinely holds the second upstream frame until the client has read the first, and asserts `X-Accel-Buffering: no`; the mechanism is `FlushInterval: -1` at agent.go:331 with `DisableCompression` at :150. Client disconnect cancels upstream -> `TestAgentRunCancelsUpstreamOnDisconnect` (agent_test.go:369), the inbound `r` (and its context) is passed straight to `proxy.ServeHTTP` (agent.go:333). Oversized body -> `bufferAgentBody` (agent.go:355) refuses with `invalid_request` before anything is dialled, `TestAgentRefusesAnOversizedBody` (:550); upstream failure -> `agentUpstreamError`/`agentFetchJSON` write a 502 `agent_upstream` that never echoes the upstream body or host (agent.go:421-451), `TestAgentUpstreamFailureIsAProblem` (:571) covers both a non-2xx and a dead upstream. Info rewriting -> `rewriteAgentInfo` + `scrubAbsoluteURLs` (agent.go:461-519), `TestAgentInfoRewritesEveryUpstreamURL` (:244). Header/query hygiene -> agent.go:303-311 and `forwardedAgentQuery` (:340), `TestAgentInjectsTokenAndStripsOrigin` (:285). `go test -race ./internal/server/...` green.
