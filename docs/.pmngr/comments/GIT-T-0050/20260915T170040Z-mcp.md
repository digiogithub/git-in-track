---
type: comment
item: GIT-T-0050
author: mcp
created: 2026-09-15T17:00:40Z
kind: comment
---

Closing the criterion "Auto-scroll follows only when pinned to the bottom; scrolling up stops it" — the behaviour was implemented in `MessageList.tsx` but nothing tested it, so the pin arithmetic and the "jump to latest" affordance were unverified.

Test added to the `MessageList` describe in `web/src/features/agent/ui/MessageBubble.test.tsx`: "offers \"jump to latest\" once the reader scrolls away from the bottom". jsdom lays nothing out, so it stubs `scrollHeight`/`clientHeight`/`scrollTop` on `data-testid="message-scroll"`, asserts nothing is offered while pinned, fires `scroll` from a position far from the bottom, finds the "Jump to latest" control, clicks it, and asserts the list scrolled back to `scrollHeight` and the control disappeared.

No production change. `npx vitest run src/features/agent src/features/boards src/api` 430/430, `tsc --noEmit`, eslint and prettier clean.
