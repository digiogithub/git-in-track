---
type: comment
item: GIT-T-0050
author: mcp
created: 2026-09-15T16:42:55Z
kind: comment
---

Closing-pass verification (read-only review of HEAD 62f03ec). Status left at `in_review`: 3 of 4 criteria met.

Met: progressive render ending as parsed Markdown -> `ui/MessageBubble.tsx:49-104` (throttled prefix + plain tail + caret), test `MessageBubble.test.tsx:13,48`; raw HTML not executed -> `MessageBubble.tsx:85` renders through `@/markdown`'s `useMarkdown`/`MarkdownContent` with `externalImages:false`, test `MessageBubble.test.tsx:24`; auto-scroll only while pinned -> `ui/MessageList.tsx:24,47-51,62-68` (`PIN_SLACK`, `onScroll`, `useLayoutEffect`) with the "Jump to latest" fallback at `:96-107`; live region -> `MessageList.tsx:80-85`, test `MessageBubble.test.tsx:88`.

**Not met:** "Vitest covers streaming render, the scroll behaviour and sanitisation" — streaming and sanitisation are covered, the **scroll behaviour is not asserted anywhere** (no test touches `data-testid="message-scroll"`; the landing comment records this as deferred because jsdom reports 0 for `scrollHeight`/`clientHeight`).

Smallest fix: one test in `ui/MessageBubble.test.tsx` that renders `MessageList`, `Object.defineProperty`s `scrollHeight`/`clientHeight` on the `message-scroll` node, sets `scrollTop` short of the bottom, fires `scroll`, and asserts the "Jump to latest" button appears — then clicks it and asserts it disappears. That exercises the real `PIN_SLACK` arithmetic without needing jsdom layout.
