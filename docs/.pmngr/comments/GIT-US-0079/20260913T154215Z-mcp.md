---
type: comment
item: GIT-US-0079
author: mcp
created: 2026-09-13T15:42:15Z
kind: comment
---

Vault operation and MCP tool landed: `youtrack.comment.push` in `internal/vault/youtrackpush.go` (routed per project by the new `Workspace.routeItem`, since the router reads `id` and this method spells its item `itemId`), and `push_comment_to_youtrack` in `internal/mcp/tools_youtrack.go`. GIT-T-0191 and GIT-T-0193 are `done`; GIT-T-0195 (CLI) and GIT-T-0197 (docs/07 + CHANGELOG) are left `todo` with the call contracts in their comments, because `cmd/gintrack`, `docs/07-cli-and-api.md` and `CHANGELOG.md` belong to other agents this wave.

Reviewer must know: `pushed[]` means **queued**, not delivered — nothing is pushed inline, the work is the job named by `jobId`, and the surfaces report that. A comment already carrying a YouTrack reference lands in `skipped[]` with its remote id rather than being sent twice. An item with no YouTrack `external` reference is refused with `invalid_request` and a message saying to import or link it first: non-retryable on purpose, since no amount of waiting creates the issue to comment on. `commentPath` goes through `s.guard.Check` before the core is asked anything.
