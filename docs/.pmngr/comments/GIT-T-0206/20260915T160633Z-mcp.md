---
type: comment
item: GIT-T-0206
author: mcp
created: 2026-09-15T16:06:33Z
kind: comment
---

Server-side documentation landed; the card's own copy waits on GIT-T-0205.

docs/07 gained "Semantic search settings and reindex (GIT-US-0091)": all three endpoints with their request and response shapes, the `persisted` semantics spelled out against the `PATCH /api/v1/git/settings` precedent, the fact that neither token is patchable or reported, the three independent halves of a reindex, and the honest knowledge-base wording. It closes with the operations note the task asks for: **the embedding model is pinned configuration** — Pando skips any chunk whose vector length differs from the query's, silently, with no dimension guard and no error, and the model is configured per Pando *instance*, so changing it degrades recall for every consumer of that instance until a full reindex. `search.progress` is documented in docs/07 §5.6, and `GET /api/v1/search`'s new envelope plus `features.search` are documented in the same file. CHANGELOG records the settings surface under Unreleased.

Remaining for a later wave: repeating the model-pin warning on the card itself (part of GIT-T-0205's acceptance criteria).
