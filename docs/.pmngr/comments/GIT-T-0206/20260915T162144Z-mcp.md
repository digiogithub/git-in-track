---
type: comment
item: GIT-T-0206
author: mcp
created: 2026-09-15T16:21:44Z
kind: comment
---

Web half of the documentation landed with the card (wave 3).

`docs/05-web-app.md`, settings screen section (§3.1, before the "Import from YouTrack" paragraph): a paragraph on `features/settings/PandoSearchCard.tsx` — what the card renders (backend badge, endpoints, project id, corpus dir, `allowRemote` with doc 07 §3.3's warning, the live probe, the per-repository corpus table), how **Reindex now** follows `search.progress` through the provider's `searchProgress` change event and re-reads the settings on a terminal frame, the `search_reindex_running` wording, the two things it refuses to do (no token field ever; a re-export is reported as `kbNote` says and never as a completed index), the instance-global embedding-model pin, and the `searchSettings` capability gate. The `Capabilities` block of §4 gained `searchSettings: boolean`.

docs/07 already carried the endpoints and the model warning from the backend half; nothing there needed changing.
