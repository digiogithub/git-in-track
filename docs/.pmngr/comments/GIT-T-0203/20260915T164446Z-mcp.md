---
type: comment
item: GIT-T-0203
author: mcp
created: 2026-09-15T16:44:46Z
kind: comment
---

Closing-pass verification, all four criteria met. Re-export + code index + progress -> internal/server/search_settings.go:610-680 `runReindex`: per repository `e.exp.ExportAll` then `client.IndexProject(repo path, id)`, with `publishProgress` (:241-261, shaped like sync.progress, topic `search.progress`, phases export/code/kb/completed/failed) and a 202 `{jobId,startedAt,phase}` from the handler :549-563 running on `context.WithoutCancel`. Concurrent call refused, running job unaffected -> `startReindex` :578-601 claims the single slot under jobMu and returns errReindexRunning, mapped to `search_reindex_running` 409 at :553-556; nothing configured at all is `search_not_configured` 400 :557-560. Partial failure isolated -> :655-671, `exportError` and `codeError` recorded per repository and the loop carries on, so a Pando outage never invalidates an export that worked; the KB half is separate and honest (:672-690, `kbNote` "Re-exported, awaiting Pando's next import pass" when no REST URL, with the KBWatch-off reasoning in the comment, and real `ReindexKB` counts when one is configured). `go test -race ./internal/server/` passes: TestSearchReindexExportsAndIndexes, TestSearchReindexRefusesASecondRun, TestSearchReindexReportsAPartialFailure, TestSearchReindexWithoutAnythingConfigured.
