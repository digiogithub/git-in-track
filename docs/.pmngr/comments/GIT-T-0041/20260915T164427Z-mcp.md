---
type: comment
item: GIT-T-0041
author: mcp
created: 2026-09-15T16:44:27Z
kind: comment
---

Closing review, all 4 criteria verified. The park leaves `runStatus: 'interrupted'`, `interrupt.toolCalls` readable off `PandoThread.pendingToolCalls` and the preceding assistant text intact -> store.test.ts:157, with the frontend-tool flavour at :195 (`isPermissionRequest` correctly false there). Resume -> store.test.ts:171 asserts the posted body directly: same `threadId`, a different `runId`, and a trailing `{role:'tool', toolCallId, content:'{"approved":true}'}` built by the SDK's `approve()`. The same test asserts the continuation rather than a restart (`messages.at(-1).text === 'Written.'`, `interrupt` cleared, status back to idle), so events after the resume append to the same list. Fixtures `permissionInterrupt`/`permissionResumed`/`frontendToolInterrupt` are checked in at web/src/features/agent/fixtures.ts:68-117. No HITL answer builder is re-implemented locally. Vitest green.
