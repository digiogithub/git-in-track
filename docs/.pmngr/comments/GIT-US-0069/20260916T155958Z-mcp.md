---
type: comment
item: GIT-US-0069
author: mcp
created: 2026-09-16T15:59:58Z
kind: comment
---

**Closed 2026-09-16.** Both criteria that held this story are met.

The companion token stopped being written in clear on 2026-09-15: `agent init` runs `pando secret` and writes an `age1:` ciphertext under `[MCPServers.gintrack.Auth]`, which Pando decrypts on load.

The live chat turn now completes. Pando's `PANDO-US-0031` fix (commit `38abda0`, branch `fix/agui-mcp-permission-binding`, PR digiogithub/pando#18) binds MCP tools to the caller's permission service instead of the process-wide one, so an AG-UI run reaches the adapter's own service. Verified end to end against a build carrying it, with the generated configuration untouched — gateway off, `AutoApprove = false`:

- `TOOL_CALL_START gintrack_list_items` → `TOOL_CALL_END` → `TOOL_CALL_START pando_permission_request` → `RUN_FINISHED{outcome:"interrupt"}`; resuming the same thread with `{"approved": true}` gave `TOOL_CALL_RESULT`, the assistant text and `RUN_FINISHED{outcome:"success"}` answering "93 stories", which matches the repository (93 stories in status `done`).
- With `AutoApprove = true` the same question completed with no interrupt at all.
- "Run `ls` with the bash tool" still produces zero tool calls: the `[AGUI] Tools` allow-list holds.

docs/20 §7 was rewritten: the `mcp_call_tool` gateway-on workaround is gone and the minimum Pando build is named. Full log on GIT-T-0118.
