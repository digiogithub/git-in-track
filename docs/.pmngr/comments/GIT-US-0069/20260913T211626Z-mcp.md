---
type: comment
item: GIT-US-0069
author: mcp
created: 2026-09-13T21:16:26Z
kind: comment
---

Rewrote the generated-template section against the 2026-09-13 decisions and the KB findings (report A4/A5/A7, A10).

Changed:
- `[Remembrances]` now generates `KBAutoImport = true` with `KBWatch = false` (the watcher erases front-matter metadata on every edit), with a comment saying why.
- `[AGUI] AllowedOrigins` is left empty — the proxy strips `Origin` — instead of listing the companion origin.
- `[AGUI]` gains `HumanInTheLoop = true` / `AutoApprove = false` now, and a `Tools` allow-list plus `Mesnada = false` once those keys exist; before that the generator writes the interim-boundary warning as a comment.
- `[MCPServer] HttpEnabled` is off by default with a comment that `:9777` is unauthenticated, CORS `*` and globally auto-approving.
- Security note corrected: loopback binding is not a boundary against the user's own browser while CORS is `*`; the ADR must list this among negative consequences.
- One `.pando.toml` per repository stated explicitly; the ADR's dependency note now names `@pando-ai/sdk/agui`, and the architecture line says per-repository `agui-serve`.
- Dropped the stale `internal/config/config.go:1137-1186` line range (kept the file as the authority).

Depends on: PANDO-EP-0002 (`[AGUI] Tools` + `Mesnada`), PANDO-EP-0006 (authenticate the MCP HTTP transport), PANDO-EP-0005 (REST reindex, replaces wait-for-auto-import in the setup docs).
