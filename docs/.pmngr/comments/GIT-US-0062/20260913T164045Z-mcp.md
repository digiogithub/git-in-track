---
type: comment
item: GIT-US-0062
author: mcp
created: 2026-09-13T16:40:45Z
kind: comment
---

`gintrack youtrack import <query | ID...>` landed in `cmd/gintrack/youtracksync.go` with `--dry-run`, `--depth`, `--comments`, `--attachments`, `--links` and `--json`, plus the `docs/07` §4.15 documentation and the corrected twenty-two-tool counts.

Two things a reviewer should know. The command dispatches `youtrack.import.preview` / `youtrack.import.run` directly rather than queueing an engine job, because those methods are synchronous in the vault — it is the REST layer that queues — so GIT-T-0123's "poll a long import" criterion is left unticked with the reasoning on the task. It reaches them through a companion built the ordinary way that simply never binds a port (`server.NewHeadless`), so the CLI shares one wiring with `gintrack serve` instead of growing a second. GIT-T-0119 is still `in_review` with another agent; its half of this story — the `gintrack mcp` long help — already lists all twenty-two tools.
