---
type: comment
item: GIT-US-0062
author: mcp
created: 2026-09-13T15:42:08Z
kind: comment
---

MCP half landed: `import_youtrack_issues` in the new `internal/mcp/tools_youtrack.go`, a write tool absent from `tools/list` on a read-only server, with `dryRun` selecting `youtrack.import.preview` over `youtrack.import.run`. Surface-pinning updated in `internal/mcp/tools_test.go`, behaviour tested through the in-memory client harness, and `docs/08-mcp-server.md` §4 and `AGENTS.md` now say twenty-two tools (7 read-only, 22 with `--allow-write`). GIT-T-0116 is `done`, GIT-T-0119 `in_review` with one criterion unticked, GIT-T-0123 (CLI) and GIT-T-0127 (docs/07 + CHANGELOG) left `todo` with the call contracts in their comments — `cmd/gintrack`, `docs/07-cli-and-api.md` and `CHANGELOG.md` belong to other agents this wave.

Reviewer must know: `cmd/gintrack/mcp_test.go` needed no change — it asserts named tools present and absent, not a count — so the four additions leave it passing. What *is* stale is the long help in `cmd/gintrack/mcp.go:33-38`, which still enumerates thirteen tools, and the drift at `docs/07-cli-and-api.md:896-916`, which now reads 22 tools / 7 read / 15 write. `tools_youtrack.go` holds all four YouTrack tools of this epic and GIT-EP-0013/0014: created once, extended, not forked.
