---
type: comment
item: GIT-T-0148
author: mcp
created: 2026-09-15T16:45:20Z
kind: comment
---

Closing review, all criteria verified: one lazily established session shared across calls and rebuilt after a transport failure -> internal/pando/client.go (acquire under c.mu with the `fresh` flag, StreamableClientTransport with DisableStandaloneSSE, isSessionDead covering ErrSessionMissing/ErrConnectionClosed/EOF/net.ErrClosed/connection reset, one reconnect per call) + TestSessionIsLazyAndShared, TestSessionReconnectsAfterRestart; timeouts honoured and typed distinctly -> context.WithTimeout per call and `classify` mapping deadline -> ErrTimeout, 401/403 -> ErrUnauthorized, rest -> ErrUnreachable, separate from ErrToolFailed (search.go:34-51) + TestTimeoutIsDistinctFromUnreachable, TestUnauthorized, TestErrorsDoNotLeakTheToken; Health returns promptly either way -> TestHealthReachable, TestHealthUnreachable, TestHealthReconnectsAfterRestart, TestCloseIsIdempotentAndFinal; no internal/core dependency -> `go list -deps ./internal/pando` lists only the package itself from internal/. `go test -race ./internal/pando/...` green against the in-process mcp.NewServer stub.
