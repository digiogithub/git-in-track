---
type: comment
item: GIT-T-0174
author: mcp
created: 2026-09-15T16:43:45Z
kind: comment
---

Closing review: two criteria met, one not — status left at `in_review`, nothing ticked.

Met: `features.search` is `"pando"` only when the backend is selected -> internal/server/server.go:504 (`"search": s.search.backend()`) and internal/server/search_settings.go:311-317 (nil searcher or `degraded` reports `"core"`), covered by `TestSearchDegradesWhenPandoFails` and `TestSearchRecoversAfterADegradation`. All four providers and the settings row handle the third value -> web/src/api/provider.ts:1209 (`'core' | 'bleve' | 'pando'`), companion-provider.ts:1169-1172 (`toFullTextSearch`), browser-provider.ts:327 (`'core'`), fake-provider.ts:1442, settings row web/src/features/settings/SettingsPage.tsx:133 (renders the value verbatim); `npx tsc --noEmit` is clean.

NOT met: "Vitest cover both values of the mapping." No Vitest exercises `features.search === 'pando'` through the companion mapping. web/src/api/companion-provider.test.ts:287,305 only asserts the `'bleve'` value, and the `'pando'` assertions in web/src/api/search-provider.test.ts:68-72 read `FakeProvider`'s own capability object, not `toFullTextSearch`/`toCapabilities`. Smallest fix: one case next to companion-provider.test.ts:2333, e.g. `expect(toCapabilities({ features: { search: 'pando' } }).fullTextSearch).toBe('pando')` and `expect(toCapabilities({ features: {} }).fullTextSearch).toBe('core')`.
