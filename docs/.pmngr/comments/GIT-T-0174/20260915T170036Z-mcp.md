---
type: comment
item: GIT-T-0174
author: mcp
created: 2026-09-15T17:00:36Z
kind: comment
---

Closing the criterion "Vitest covers both values of the mapping". The mapping itself was already right (`toFullTextSearch` in `web/src/api/companion-provider.ts:1169`); nothing asserted the third value, so a regression to the old two-value ternary would have been silent.

Test added in `web/src/api/companion-provider.test.ts`, next to the capabilities block: "maps features.search onto fullTextSearch, pando included" — asserts `'pando'`, `'bleve'`, `'core'`, an absent `features.search`, and an unknown backend (`'lucene'`) falling back to `'core'` rather than promising a surface that is not there.

No production change. `npx vitest run src/api` green (430/430 across the three suites run), `tsc --noEmit`, eslint and prettier clean.
