---
type: comment
item: GIT-T-0147
author: mcp
created: 2026-09-13T16:23:48Z
kind: comment
---

Closed after verification rather than implementation: this task was already satisfied by `cf1cad4` and had simply not been marked.

`core.Comment.External` is declared at `internal/core/model.go:455`; `ParseComment` reads it at `internal/core/frontmatter.go:333` and `SerializeComment` writes it at `:438`, both through the shared `externals` reader and writer used for items, so the canonical key order is the same one. `Comment.Extra` still preserves unknown keys. `internal/core/testdata/comment.md` covers the round trip, `go test -race ./internal/core/...` passes and the package still builds for `js/wasm`.
