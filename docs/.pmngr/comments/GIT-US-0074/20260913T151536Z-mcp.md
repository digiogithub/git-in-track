---
type: comment
item: GIT-US-0074
author: mcp
created: 2026-09-13T15:15:36Z
kind: comment
---

Server half landed in `internal/server/syncjobevents.go` (new): the observer hangs off `syncengine.Options.OnJob`, which already existed, so `internal/syncengine` needed no change and still imports nothing from this package. Five topics documented in `docs/07-cli-and-api.md` §5.6.

Two things for the next agent. **`sync.job.started` never fires yet** — `internal/syncengine` does not call `OnJob` for the queued → running transition (`startBatch` transitions and returns without emitting); the mapping and the documentation are in place and it needs one `e.emit(...)` line in a package this story does not own. And **GIT-T-0137 (`useSyncJobEvents`) is untouched and still `todo`**: it is a `web/` change and `web/` belongs to another agent this wave. That is why this story is `in_review` and not `done`.
