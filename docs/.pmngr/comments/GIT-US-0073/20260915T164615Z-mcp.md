---
type: comment
item: GIT-US-0073
author: mcp
created: 2026-09-15T16:46:15Z
kind: comment
---

Closing review. All five child tasks (GIT-T-0122, 0128, 0130, 0134, 0138) are now `done`. The story moves `in_progress` -> `in_review` rather than `done`, because one criterion is not proven the way it asks. Nothing is ticked.

Verified: full export on start under `<corpusDir>/<repo id>/<project>/items|kb`, nothing inside the repository -> internal/server/search_settings.go:196-207 and internal/pandosync/exporter.go:206-212 (`New` refuses a git working tree) + TestCorpusExportsOnStartWithoutBlockingTheListener, TestExportAllWritesTheCorpusLayout. Front matter with id/type/status/milestone/parent/project/updated and a `tags` list leading with the id, plus the id repeated in the body text -> document.go:23-71 + TestRenderItemFrontMatterAndIdentityLine. `item.changed` rewrites exactly that file and a deleted item's file is removed -> pandosync_hub.go:138-166 and exporter.go:334-351 + TestCorpusFollowsHubEvents, TestApplyItemChangedRewritesExactlyOneFile, TestApplyRemovals. `file.changed` with `IsKb` rewrites the page -> pandosync_hub.go:150-162 + TestCorpusChangeOf, TestApplyPageChanged. Atomic and debounced writes -> exporter.go:498-513 and the 250 ms coalescing map in pandosync_hub.go:82-119 + TestWritesAreAtomic, TestApplyBurstOverTheSameItemWritesOnce. An unchanged second export writes nothing -> exporter.go:474-492 + TestExportAllSkipsUnchangedDocuments, TestExportAllSkipsFromDiskAfterRestart. Paths cannot escape the corpus root -> paths.go:245-297 + TestPagePathRefusals, TestExportAllRefusesPathsThatEscapeTheCorpus. 10 000 items inside budget and off the startup path -> TestExportAllLargeCorpusStaysWithinBudget plus the goroutine at pandosync_hub.go:22-28. `go test -race ./internal/server/... ./internal/pandosync/` green. The KBWatch=false decision is honoured: the exporter never uses fsnotify and docs/21 §4 records it.

NOT met: "**A hub subscriber overflow triggers a full re-export rather than a silent desync**, and the test drives an overflow to prove it." The code is there — `drainHub` returns on `client.overflowed` and `followHub` unregisters, runs `Apply(Overflow)` on every exporter and re-subscribes (internal/server/pandosync_hub.go:65-78, :99-119) — but no test drives a real overflow. TestCorpusReExportsAfterAnOverflow (pandosync_hub_test.go:153-179) calls `s.search.applyToAll(ctx, Event{Kind: Overflow})` directly and never touches the hub, and TestApplyOverflowTriggersAFullExport injects the same event at the exporter. So the branch that actually matters — the hub's slow-subscriber drop at hub.go:113-141 reaching `followHub`, and the re-subscription afterwards — is unexercised, which is exactly the silent-desync path the criterion was written for.

Smallest fix: in internal/server/pandosync_hub_test.go, start `followHub` against the real hub, publish more than `clientBuffer` `item.changed` events without draining so `deliver` -> `markOverflow` closes `overflowed`, then assert a corpus file deleted behind the exporter's back comes back and that the hub has a registered client again afterwards. Roughly fifteen lines; no production change needed.
