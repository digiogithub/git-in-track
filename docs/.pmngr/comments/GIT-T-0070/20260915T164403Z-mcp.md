---
type: comment
item: GIT-T-0070
author: mcp
created: 2026-09-15T16:44:03Z
kind: comment
---

Closing-pass verification (read-only review of HEAD 62f03ec). All four criteria met.

Tool name + arguments as escaped text, three actions, nothing defaulting to yes -> `ui/PermissionDialog.tsx:97-131` (`<dl>` and a `<pre data-testid="permission-params">`, never Markdown/HTML) and `:161-181`, test `PermissionDialog.test.tsx:30,41,49,58`. Escape and backdrop both resolve to a denial -> `PermissionDialog.tsx:70-73` (`onOpenChange`, Radix reports Escape and a backdrop click through it) and `:80-84` (`onInteractOutside` prevented, same path), landing in the confirm-then-deny alertdialog at `:139-158`; the dialog never closes without an answer reaching Pando, tests `PermissionDialog.test.tsx:66,78`. Approve-always is per-thread, in memory, and suppresses the next prompt for the same tool -> `ui/InterruptSlot.tsx:108-110` -> `store.ts:486-490` `allowToolForThread` -> `store.ts:313-319` `settle()` auto-approving only a permission whose underlying `toolName` is granted; cleared by `attach`/`newThread`/`selectThread` (`store.ts:362,384,405`) and never written to storage — tests `store.tools.test.ts:220,241` and `AgentPage.test.tsx:236` (revocable badge in the rail). Read-only tab offers no answer at all -> `PermissionDialog.tsx:133-137`, test `:89`. Suite green (17 files / 162 tests).
