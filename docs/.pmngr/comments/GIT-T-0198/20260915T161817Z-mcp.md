---
type: comment
item: GIT-T-0198
author: mcp
created: 2026-09-15T16:18:17Z
kind: comment
---

Skill done, docs/20 mirror pending (not my file this wave), so this stays in_progress.

`cmd/gintrack/templates/agent/skill.md.tmpl` now carries a four-row routing table with one example each: **Structured** (`get_item`, `list_items`), **Literal** (`search_items`, `search_kb`), **Semantic** (`search_semantic`, fallback `kb_search_documents` with a `path_prefix`), **Code** (`code_hybrid_search` then `code_find_symbol`). The semantic example uses `search_semantic(query:…, limit: 5)` and spells out the `unavailable` refusal: the query never ran, so fall back to the literal tools rather than report that nothing matched. The `hybrid_search_remembrances` note (blended, incommensurable ranking, hides which index answered, no `path_prefix`) is kept and still asserted by `cmd/gintrack/agent_test.go`. Identical table in `docs/08-mcp-server.md` §4.19.

Needed in `docs/20-agent-interface.md` §5 (owner of that file, please apply):
- add a row to the tools table: `| `gintrack_search_semantic` | "Which stories or pages are about X" — meaning, not wording | Ranks items and pages by embedding; needs the Pando backend under `search.pando`, and without one it answers `unavailable` naming `search_items` as the fallback — never an empty list. |`
- change the `gintrack_list_items, gintrack_get_item, gintrack_search_items` row so `gintrack_search_items` reads as the *literal* route, and mark `kb_search_documents` as the corpus-level fallback to `gintrack_search_semantic`.
- one line saying the skill file (§3 table, `agents/skills/gintrack-search/SKILL.md`) carries the same four rows.

Also worth adding to `cmd/gintrack/agent_test.go` (not my file): `"search_semantic"` in the skill assertion list around line 90.
