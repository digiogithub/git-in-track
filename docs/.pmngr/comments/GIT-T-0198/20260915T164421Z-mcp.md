---
type: comment
item: GIT-T-0198
author: mcp
created: 2026-09-15T16:44:21Z
kind: comment
---

Closing-pass verification, all three criteria met, including the docs/20 mirror this task's own comment left open — a later wave applied it. Four-row table with one example per row -> cmd/gintrack/templates/agent/skill.md.tmpl:17-20 (Structured / Literal / Semantic / Code) with the examples at :26, :36, :45-55 (`search_semantic(query:…, limit: 5)` plus the `unavailable` fallback wording) and :73-76; asserted by cmd/gintrack/agent_test.go:91 which now pins `search_semantic`, and `go test -race ./cmd/gintrack/` passes. Same table in docs/20-agent-interface.md:255-262 with the `gintrack_search_semantic` row, `search_items` read as the literal route and `kb_search_documents` as the corpus-level fallback, plus the line saying the generated skill carries the same four rows (:264). The `hybrid_search_remembrances` note is present in both -> skill.md.tmpl:80-86 and docs/20:266-268 (blends memories, KB and code into one ranking, hides which index answered, takes no `path_prefix`); mirrored again in docs/08-mcp-server.md §4.19 (:906).
