---
item: GIT-US-0152
author: claude
created: 2026-09-24T22:37:17Z
type: comment
---

Implemented in jj workspace `us-0152` (on main + independent fixes, not the Phase 11 stack).

- `core.ItemPatch.conflictWith`: a patch `applyPatch` refuses (blank title, unknown
  `unset` field) now reports every field it carries via `carriedFields` instead of `nil`.
- `diffFields` also compares `custom`, `external` and `inbox` (named, never quoted), so a
  stale write to those alone no longer returns an empty `conflicts[]`.
- Tests (already applied / partial overlap / refused): core `TestUpdateReportsTheFieldsInConflict`,
  vault `TestStaleItemUpdateConflicts`, MCP `TestStaleUpdateNeverReportsARefusedPatchAsApplied`,
  HTTP `TestStalePatchNeverReportsARefusedPatchAsApplied`; the refused cases fail without the fix.
- Docs: docs/08 §4.5, docs/03 R-REV-3a, CHANGELOG Unreleased/Fixed.

`make lint`, `make test`, `make wasm` pass.
