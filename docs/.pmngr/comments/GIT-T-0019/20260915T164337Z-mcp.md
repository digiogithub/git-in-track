---
type: comment
item: GIT-T-0019
author: mcp
created: 2026-09-15T16:43:37Z
kind: comment
---

Closing review, all 3 criteria verified; the web half that held this in_progress has since landed, so it goes to done. `features.agent` true only with both halves -> `s.agent.available()` (internal/server/server.go:532, agent.go:102), `TestAgentCapabilityFlag` (agent_test.go:513) and `TestAgentDisabledReportsNotImplemented` (:489). Flag and banner -> cmd/gintrack/serve.go:104 (`--agent`, default false), :196 (`pickBool` over `agent.enabled`), :374-376 (target line, or a warning with no URL); `TestServeAgentFlagIsDeclared`, `TestBannerPrintsTheAgentTargetWithoutItsToken`, `TestBannerWarnsWhenTheAgentHasNoUpstream` (serve_test.go:304, :327, :366). Web side -> `Capabilities.agent` (web/src/api/provider.ts:1237, default false at :2404), `toCapabilities` maps `features.agent` (companion-provider.ts:1200), browser-provider.ts:334 reports false; Vitest covers the mapping at companion-provider.test.ts:2140 and browser-provider.test.ts:781. `npx tsc --noEmit` clean and `npm run build` succeeds, so all providers compile against the widened type.
