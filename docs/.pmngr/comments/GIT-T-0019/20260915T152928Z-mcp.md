---
type: comment
item: GIT-T-0019
author: mcp
created: 2026-09-15T15:29:28Z
kind: comment
---

Go half landed; left **in_progress** because the web half is outside this wave's file ownership.

Done: `features.agent` in `handleCapabilities` (`internal/server/server.go`), true only when `Options.Agent` is set **and** an upstream URL is configured (`agentState.available()`). `cmd/gintrack/serve.go` gains `--agent` (default false, `pickBool` over `agent.enabled`), passes `Agent` and `Pando: cfg.PandoTargets()` into `server.Options`, and prints a banner line `agent:      <url><path> (<agent name>)` — the URL and the agent name, never the token; with `--agent` and no URL it prints a warning instead of a target. Tests: `TestAgentCapabilityFlag`, `TestAgentDisabledReportsNotImplemented` (both halves of the flag), `TestServeAgentFlagIsDeclared`, `TestBannerPrintsTheAgentTargetWithoutItsToken`, `TestBannerWarnsWhenTheAgentHasNoUpstream`.

Not done here: `web/src/api/provider.ts` (`Capabilities.agent`), `companion-provider.ts` `toCapabilities`, `browser-provider.ts` and `fake-provider.ts`. `web/` belongs to the AG-UI client wave (GIT-US-0053); the server contract they need is simply `capabilities.features.agent: boolean`. Move this task to in_review once that lands.
