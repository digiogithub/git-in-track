---
type: comment
item: GIT-US-0068
author: mcp
created: 2026-09-13T16:22:06Z
kind: comment
---

GIT-T-0150 is closed: `docs/03-data-model.md` §11 gains R-CMT-5 (why the comment's `external` entry is what makes a push idempotent) and R-CMT-6 (a local delete never deletes remotely, a comment deleted upstream is left alone locally, and re-pushing after removing the entry produces a second remote comment); §18 gains a `comment.schema.json` outline; and ADR-031 records the delete asymmetry under "Harder — the price we are paying".

**The story is not closed**, because GIT-T-0147 ("Add the external field to core.Comment") is still `todo`. It appears to be stale rather than outstanding: `core.Comment` already carries `External []External` (wave 1, `cf1cad4`, `internal/core/model.go`), and the push path in `internal/server/youtrackcomments.go` depends on it. Whoever owns `internal/core` should confirm and close it; the docs agent did not want to close a core task on a reading of the struct alone.
