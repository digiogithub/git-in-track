---
type: comment
item: GIT-T-0176
author: mcp
created: 2026-09-15T16:06:13Z
kind: comment
---

Landed. docs/02 §8 keeps the bleve paragraph and adds **§8.1 "Pando, the optional semantic accelerator"**: what it adds (hybrid lexical plus embedding ranking over the exported corpus), the candidates-only contract, why it stays optional (browser-only mode has no reach and `internal/core` must compile to WASM, ADR-003 — browser mode always uses the core index), why the two score spaces are never merged into one ranking, and the two ways a client learns which backend answered (`features.search`, and `source` plus `degraded` on each answer). The bleve sentence now says explicitly that it is documented, not shipped. docs/07 documents the `features.search` values and the new response envelope; CHANGELOG has the entry under Unreleased.
