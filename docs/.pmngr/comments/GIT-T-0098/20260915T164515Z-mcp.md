---
type: comment
item: GIT-T-0098
author: mcp
created: 2026-09-15T16:45:15Z
kind: comment
---

Closing review, all four criteria verified: interrupt executes and resumes with the step visible -> `settle()` in `web/src/features/agent/store.ts:290-306` runs the registered runner and re-POSTs the same thread with a trailing `tool` message (declarations re-sent on `send`/`resume`, :428-432, :447-451), test `store.tools.test.ts:120` asserts same `threadId`, `role: 'tool'`, `toolCall.status === 'done'`; unknown name or invalid arguments resume with an error and no side effect -> `createToolRunner().run` always encodes a result (`tools/registry.ts:118-140`), tests `store.tools.test.ts:167` and :182, both with `navigate` never called; navigating away does not lose the thread -> the resume POST is issued from `settle` before the page unmounts, the active thread id is persisted per repo and `AgentPage` re-attaches and `hydrate()`s on mount (`ui/AgentPage.tsx:127-150`), transcript restored from the server in `store.tools.test.ts:275` (weakest link: no test drives unmount mid-execution); vitest -> `npx vitest run src/features/agent/store.tools.test.ts` green.
