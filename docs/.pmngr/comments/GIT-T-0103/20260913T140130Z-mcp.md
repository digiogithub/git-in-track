---
type: comment
item: GIT-T-0103
author: mcp
created: 2026-09-13T14:01:30Z
kind: comment
---

Core half landed; the `internal/vault` half is left for whoever owns that file this wave — I was scoped to `internal/core/sprint*.go` only and did not touch `internal/vault/sprint.go`.

Ready to call from `checkSprintDates`:

- `(*core.Sprint).IsDraft()` — true when either date is absent. A draft must be accepted unconditionally, so guard the whole overlap check with it.
- `core.OverlappingSprint(candidate, others) *Sprint` — already existed and already returns nil for a dateless candidate *or* a dateless other (pinned by `TestDraftSprintsNeverOverlap`).
- `core.SprintOverlapMessage(candidate, other) string` — the refusal sentence, naming both sprints, the other's range, the board, and the escape hatch ("remove them to keep `<id>` a draft"). Wrap it in the vault's `sprint_overlap` error.

The on-disk `W-SPRINT-OVERLAP` warning in `ValidateSprintSet` is unchanged, and a draft raises none.
