---
type: comment
item: GIT-US-0049
author: mcp
created: 2026-09-15T16:44:56Z
kind: comment
---

Closing review: all five child tasks are done and all 14 story criteria verified against the code. Config -> `TestPandoSectionRoundTrips`/`TestPandoEnvOverridesFromResolve` (internal/config/pando_test.go:70,:169). Routing table (repo-keyed, the recorded 2026-09-15 deviation from "projectId") -> `agentUpstream` internal/server/agent.go:374-395 with a typed `agent_repo_unknown` 404 and no fallback, `TestAgentRunRoutingTablePerRepo`/`TestAgentUnknownRepoIsA404` (agent_test.go:680,:533). Info rewrite -> `rewriteAgentInfo`+`scrubAbsoluteURLs` (agent.go:461-519), `TestAgentInfoRewritesEveryUpstreamURL` (:244). Unbuffered SSE with `FlushInterval: -1` and server-side token injection -> agent.go:309-331, `TestAgentRunStreamsFrameByFrame` (:313, holds frame two until frame one is read) and `assertNoTokenLeak` over body, headers and log. Origin stripped and no `?token=` on either hop -> agent.go:303-311 + `forwardedAgentQuery` (:340), `TestAgentInjectsTokenAndStripsOrigin` (:285). Inside `s.bearerAuth` -> api.go:110, `TestAgentRequiresCompanionToken` (:471); timeout exemption -> server.go:462-466, `TestAgentRoutesAreExemptFromTheRequestTimeout` (:652). Cap -> `acquire()` (agent.go:108) refusing with 503 + `Retry-After: 5`, configurable as `agent.pando.maxRuns`, `TestAgentRunCapRefusesWithRetryAfter` (:411); per-user cap superseded by the global-only decision of 2026-09-15 (one identity today). Cancel-on-disconnect -> `TestAgentRunCancelsUpstreamOnDisconnect` (:369). 1 MiB refusal -> `bufferAgentBody` (agent.go:355), `TestAgentRefusesAnOversizedBody` (:550). `features.agent` both halves -> server.go:532, `TestAgentCapabilityFlag` (:513). Health probe superseded: `/api/v1/agent/health` -> upstream `{path}/healthz`, which exists in Pando v0.705.1, so the `/info`-as-probe interim is dropped and PANDO-EP-0004 is not blocking; `TestAgentHealthNeedsNoUpstreamToken` (:600). Upstream failures -> RFC 7807 via `agentUpstreamError` (:440), `TestAgentUpstreamFailureIsAProblem` (:571). Docs -> docs/07-cli-and-api.md §3605 plus the config, flag and capability sections, and the CHANGELOG entry has landed. `go test -race ./internal/config/... ./internal/server/... ./cmd/gintrack/...` all green.

One residual note, not a blocker: a failure that happens *after* the first SSE frame has been written cannot become a problem document, and `httputil.ReverseProxy` ends it by aborting the connection rather than emitting a terminal `RUN_ERROR` frame. The story mandates `ReverseProxy` over a hand-rolled loop, so injecting that frame would mean a custom copy; the browser side already treats a mid-stream transport failure as terminal (web store.test.ts:218). Worth a follow-up only if a client ever needs the frame rather than the broken stream.
