---
type: comment
item: GIT-US-0049
author: mcp
created: 2026-09-13T21:15:16Z
kind: comment
---

Aligned with the 2026-09-13 deployment and security decisions.

Changed:
- Upstream is now a `projectId -> {url, token}` routing table over several per-repository `pando agui-serve` processes, not a single Pando URL. New criterion for unknown-project handling.
- Header hygiene made explicit: strip the browser `Origin` (Pando's `AllowedOrigins` stays empty), never a `?token=` query parameter in either direction, token injected as `Authorization` server-side only.
- Streaming requirement pinned to `httputil.ReverseProxy` with `FlushInterval: -1` instead of "flush every frame".
- Added the per-user and global in-flight concurrency cap with an RFC 7807 refusal plus `Retry-After` — Pando enforces no limit of its own.
- Health probe documented as `GET /api/v1/agui/info` until a real healthz route exists.
- Line reference refreshed: `internal/server/server.go:411-435` -> `:444` (`handleCapabilities`; `"search": "core"` sits at `:459`), verified in the tree.

Depends on: PANDO-EP-0004 (operability — replaces the `/info` health probe with a real healthz). PANDO-EP-0003 is related but does not block: cancel-on-disconnect is the correct behaviour today.
