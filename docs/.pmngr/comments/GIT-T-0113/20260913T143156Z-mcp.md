---
type: comment
item: GIT-T-0113
author: mcp
created: 2026-09-13T14:31:56Z
kind: comment
---

Drift warning for whoever reviews this. The *As built* notes in `docs/04` §8.2 and §12.1 are pinned to `cf1cad4`. While this pass was running, another agent was extending `internal/vault/sprint.go` in the same working tree — `sprint.transfer`, a dry-run close and a new `sprint_target_completed` code that reads the derived status. If that work also relaxes `checkSprintDates`, writes the snapshot on close or teaches `sprint.list` to order by derived status, the three *As built* paragraphs (R-SPR-9, R-SPR-10, R-MET-12 and the note under the snapshot example) become stale in that same change and should be deleted rather than edited: the surrounding rules are already written as the intended behaviour.
