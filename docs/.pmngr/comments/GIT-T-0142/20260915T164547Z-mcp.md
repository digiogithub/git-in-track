---
type: comment
item: GIT-T-0142
author: mcp
created: 2026-09-15T16:45:47Z
kind: comment
---

Closing review: three of four criteria hold, one fails, so the status stays `in_review` and nothing is ticked.

Met: the section round-trips through parse and save and is validated -> internal/config/config.go:466-503 (Search/SearchPando with its redacting String) and internal/config/validate.go:86, :192-199 + TestParsePandoSections, TestPandoSectionRoundTrips, TestPandoDefaults. Non-loopback refused without the opt-in and accepted with it -> validate.go:194-199 delegating to validatePandoURL:204-228 (scheme, host, no inline credentials, `!allowRemote && !LoopbackURL`) and the same guard again in the client at internal/pando/client.go:310-324 + TestValidatePandoRefusals (search.pando.mcpUrl case), TestValidatePandoAllowRemote, TestLoopbackURL, TestNewRefusesNonLoopbackURL, TestNewAllowRemoteIsTheOnlyOverride. `go test -race ./internal/config/...` and `./internal/pando/...` green.

NOT met: "The derived default `project_id` matches Pando's sanitisation for several sample paths". Nothing derives it. `pando.SanitizeProjectID` (internal/pando/client.go:331-353, a faithful port covered by TestSanitizeProjectID over eight paths) has no caller outside its own test: `grep -rn SanitizeProjectID --include=*.go` returns only client.go and client_test.go. The configured value is passed through verbatim at internal/server/search_settings.go:156-162, and with it empty `SearchCode` answers `ErrNotConfigured` (internal/pando/search.go:198-203). That contradicts both the documentation and the field comment, which promise a derived default: docs/07-cli-and-api.md:298 ("empty = Pando's sanitised repo path") and internal/config/config.go:490-491 ("Empty means Pando's own sanitized repository path"). Consequently the fourth criterion also only half holds: the config tests cover the round trip and the loopback refusal, not the sanitisation, which is tested in internal/pando instead.

Smallest fix: in `searchState.rebuild` (internal/server/search_settings.go:153-162), when `settings.ProjectID` is empty pass `pando.SanitizeProjectID(<the workspace mount's path>)` instead of ""; add one table test over a few sample paths next to the existing config tests, or amend docs/07:298 and config.go:490-491 to say that an empty `projectId` disables code search rather than deriving one.
