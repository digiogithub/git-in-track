---
type: comment
item: GIT-T-0192
author: mcp
created: 2026-09-15T16:17:30Z
kind: comment
---

Already satisfied by the wave-2 semantic-search work; verified, not re-implemented.

`search.semantic` is a case of the workspace dispatch table (`internal/vault/dispatch.go:302-311`), decoding `vault.SemanticQuery{Q, Limit, Project, Kind}` and answering `[]vault.SearchHit` (`kind,id,path,title,snippet,score,source,project,vaultId`). The business logic and the host seam live in `internal/vault/semantic.go`: `SemanticSearcher`, `Workspace.SetSemanticSearcher`, `SemanticAvailable`, `SearchSemantic` — which returns `vault.Error{Code:"unavailable"}` when no backend is installed, never core results. `internal/mcp` holds none of it.

Reviewer: `internal/vault/semantic.go`, `internal/vault/dispatch.go:302`. The MCP side that consumes it is GIT-T-0194.
