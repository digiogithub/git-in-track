---
type: comment
item: GIT-T-0192
author: mcp
created: 2026-09-15T16:43:47Z
kind: comment
---

Closing-pass verification, all three criteria met: dispatchable with the documented shape -> internal/vault/dispatch.go:302-311 decoding `SemanticQuery` into `Workspace.SearchSemantic` (internal/vault/semantic.go:74-95), hits carrying kind/id/path/title/snippet/score/source/project/vaultId (semantic.go:99-113); typed `unavailable` and never core results -> semantic.go:76-79 `failf("unavailable", …)`, asserted by TestSemanticSearchNeedsABackend; `go test -race ./internal/vault/` covers the hit (TestSemanticSearchNamesTheRepository, incl. the dispatch round trip) and the unavailable backend — the empty-result case is exercised through the same `Workspace.SearchSemantic` path by internal/mcp `TestSearchSemantic/an_empty_result_is_an_empty_list,_not_a_failure` rather than by a vault-package test, so the behaviour is covered but a two-line empty case in internal/vault/corpus_test.go would put it where the criterion names it.
