---
type: comment
item: GIT-US-0090
author: mcp
created: 2026-09-13T16:22:11Z
kind: comment
---

GIT-T-0215 is **partially** done and left `in_review`. The knowledge-base half landed in `docs/03-data-model.md` §14.6: the five states, why change detection is content-based (the `## Feedback` block is rewritten on every write and never leaves the repository, so bytes are the wrong comparison), that `remote_ahead` is only reported when the caller asked for the remote read, and the conflict semantics — last writer wins per direction, both changed writes `<page>.conflict.md` with a `conflict_of` key and leaves the page untouched, no three-way merge. `youtrack.kb.conflict` was already in `docs/07-cli-and-api.md` §5.6.

What is left is the first criterion: the three routes and the per-page status shape in `docs/07-cli-and-api.md` §4/§5.5. At `7d8d9a3` those routes did not exist at all — `mountYouTrack` served settings, test, projects, fields, issues and the two import routes and nothing else — and GIT-T-0214 landed them while this pass was running. `docs/07-cli-and-api.md` belongs to another agent this wave, so the entry belongs with GIT-T-0214's author rather than here. **The story should not be closed until that is written.**
