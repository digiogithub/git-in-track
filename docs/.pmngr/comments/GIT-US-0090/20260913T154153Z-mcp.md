---
type: comment
item: GIT-US-0090
author: mcp
created: 2026-09-13T15:41:53Z
kind: comment
---

Vault half landed (`internal/vault/youtrackkb.go`, routed in `dispatch.go`): `youtrack.kb.status`, `youtrack.kb.publish` and `youtrack.kb.pull`, with a new host seam `SetYouTrackEnqueuer` so the vault queues jobs and never runs one. GIT-T-0212 and GIT-T-0213 are `done`; GIT-T-0214 (REST) and GIT-T-0215 (docs/07, CHANGELOG) are left `todo` with the call contracts written out in their comments, because `internal/server`, `docs/07-cli-and-api.md` and `CHANGELOG.md` belong to other agents this wave.

Reviewer must know: status is offline by default and reads articles only under `remote: true` — one request per page otherwise, which is how a tree view becomes a rate-limit incident. Change detection is `mapping.EqualContent` plus a fingerprint in `external.key` taken over the published content, never a byte comparison: `core.PruneKbFeedback` rewrites the `## Feedback` block on every write and the block never leaves the repository, so raw bodies would report a remote edit forever (ADR-030, R-FB-4). `TestYouTrackKBStatusIgnoresTheFeedbackBlock` pins that.
