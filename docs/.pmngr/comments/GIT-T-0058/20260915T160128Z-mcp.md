---
type: comment
item: GIT-T-0058
author: mcp
created: 2026-09-15T16:01:28Z
kind: comment
---

Done. `ui/Composer.tsx` over `textarea.tsx` and `button.tsx`. Enter sends, Shift+Enter breaks the line (and an IME composition never sends), the field auto-grows to 192 px then scrolls, Send is disabled while the draft is blank, and Stop replaces it during a run and calls the store's `cancel`.

The draft survives everything except a successful send: stopping, a refusal and an interrupt all keep it. Sending is blocked with a `role="status"` explanation when a run is parked on a question ("The agent is waiting for an answer above…") and when another tab owns the conversation, where the field is disabled outright. The keyboard contract is the field's `aria-describedby`.

Tests cover Enter/Shift+Enter, the whitespace refusal, Stop with the draft preserved, the interrupted block, the read-only state and the accessible description.
