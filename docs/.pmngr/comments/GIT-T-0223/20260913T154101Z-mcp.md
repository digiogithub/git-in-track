---
type: comment
item: GIT-T-0223
author: mcp
created: 2026-09-13T15:41:01Z
kind: comment
---

Left `todo`: half done, and the other half is in files owned by other agents this wave.

Done: `docs/08-mcp-server.md` §4.18 documents `publish_kb_page_to_youtrack` and `sync_kb_page_from_youtrack` together — input/output example, the path-guard refusal, and the three rules that govern what crosses the boundary in both directions: the title lives only in the article summary; the `## Feedback` block never leaves the repository and its absence upstream is never evidence of a remote change (ADR-030); a page both sides changed produces `<page>.conflict.md` and nothing is merged. The table rows and the twenty-two-tool counts are updated there and in `AGENTS.md`.

Still open, in `docs/07-cli-and-api.md` §4 and `CHANGELOG.md`: `gintrack youtrack kb push|pull` with `--recursive`, `--wait` and `--json`, and the non-zero exit on a conflict. The commands themselves are unwritten (GIT-T-0222).
