---
type: comment
item: GIT-T-0047
author: mcp
created: 2026-09-15T16:01:13Z
kind: comment
---

Done. `agentRoute` in `web/src/app/router.tsx` (`/agent`, `lazyRouteComponent(() => import('@/features/agent'), 'AgentPage')`, registered before `settingsRoute`). Sidebar: `agentNavItem` in `AppShell.tsx`, spliced into `mainNavItems` only when `capabilities.agent` — `navItems` no longer holds Settings, so optional entries land above it.

The route always resolves; `AgentPage` itself renders `AgentUnavailable` when the capability is false, so a stale link never 404s. Tests: `web/src/app/router.test.ts` (the path is registered once) and two new cases in `AppShell.test.tsx` covering both capability states.
