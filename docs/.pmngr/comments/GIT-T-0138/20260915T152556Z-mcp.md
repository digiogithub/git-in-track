---
type: comment
item: GIT-T-0138
author: mcp
created: 2026-09-15T15:25:56Z
kind: comment
---

Landed as a new document `docs/21-semantic-search.md`, with its row added to the table in `docs/README.md`.

It covers the corpus layout, the document shape and why the id is repeated in the body, the derived-data status (safe to delete, never committed), the `[Remembrances] KBPath` + `KBAutoImport=true` + `KBWatch=false` configuration, the overflow → full re-export rule, the Go seam for wave 2, and an operating-notes table.

The `KBPath` warning has its own subsection (§4.1) directly under the TOML block a reader copies, stating that Pando's walk has no hidden-directory or `node_modules` exclusion and will exhaust inotify watches if pointed at a repository root.

Not done here: the `CHANGELOG.md` entry. `CHANGELOG.md` is outside this wave's ownership; the exact patch is in the implementation report for the coordinator to apply. The task body also named `docs/07-cli-and-api.md` and the agent-interface document as the homes for this; a dedicated `docs/21` was chosen instead so the Pando corpus is documented in one place rather than split across two documents owned by other waves.
