---
type: comment
item: GIT-US-0091
author: mcp
created: 2026-09-15T16:45:49Z
kind: comment
---

Closing pass: all four child tasks are done and all ten story criteria are met. GET returns the seven fields -> internal/server/search_settings.go:343-429. PATCH persists and returns `persisted`, no ConfigPath -> process only -> :447-517,529-546 (TestSearchSettingsPatchPersists, TestSearchSettingsPatchWithoutAConfigFile). Reindex re-exports, triggers code_index_project, publishes progress and refuses a second run -> :549-601,610-671 and `publishProgress` :241-261 (TestSearchReindexExportsAndIndexes, TestSearchReindexRefusesASecondRun). KB half reported honestly -> :672-690 `kbNote` "Re-exported, awaiting Pando's next import pass" when no REST URL. The Pando REST reindex route is now available and is called -> internal/pando/rest.go:41-49 `POST /api/v1/remembrances/kb/reindex`, its scanned/added/updated/unchanged/deleted counts shown on the card at PandoSearchCard.tsx:482-486 (so the criterion that was written as future work is satisfied today). Card renders status, saves, reindexes with progress and names an unreachable Pando -> PandoSearchCard.tsx:127-243,391-419,466-490, SettingsPage.tsx:120. Hidden without the capability -> :127, browser-provider.ts:333. Embedding-model pin visible and instance-global -> :515-519, repeated in docs/07:2923-2929 and docs/05:530-552. Tests: `go test -race -count=1 ./internal/server/` passes (all TestSearch* green); `npx vitest run src/features/settings/PandoSearchCard.test.tsx src/api` -> 209 passed. Docs and CHANGELOG -> docs/07:2833-2929, docs/07 §5.6 :3970, docs/05:530-552,798, CHANGELOG.md:60-78.
