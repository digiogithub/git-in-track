---
type: comment
item: GIT-US-0091
author: mcp
created: 2026-09-13T21:19:22Z
kind: comment
---

Pulled the backend half forward and made the reindex button honest.

Changed:
- The settings and reindex endpoints are now sequenced next to GIT-US-0077 instead of last in the epic; only the card stays at the end. They are what make the exported corpus trustworthy.
- Split "reindex now" into two halves. Code half: `code_index_project` now, with a note that `POST /api/v1/remembrances/projects/index` already exists over plain REST with `X-Pando-Token` and is simpler than the MCP path; `code_index_status(job_id)` gives progress. KB half: re-export the corpus and wait for Pando's next auto-import pass — `KBWatch` is off by design, so there is no watcher to trigger — reported as "re-exported, awaiting import", not as a completed reindex. New criteria for both, plus one for switching to the REST reindex route and showing its scanned/added/updated/deleted counts when it lands.
- Strengthened the embedding-model warning: the model is global to the Pando instance, so a change degrades recall for every consumer of it, not just gintrack.

Depends on: PANDO-EP-0005 (REST KB reindex route). Nothing else blocks; the backend half is buildable today.
