---
type: comment
item: GIT-US-0064
author: mcp
created: 2026-09-15T17:01:11Z
kind: comment
---

Done: the last open child, GIT-T-0086, is closed. `focus_board_card` was selecting an attribute nothing rendered, so the one tool that drives the *board* never found a card; board cards now carry `data-item-id` next to `data-ref` and the selector matches it, with a jsdom test over the real card markup. A latent `CSS.escape` receiver bug was fixed in the same executor. All five children (GIT-T-0082, 0086, 0090, 0094, 0098) are done; `npx vitest run src/features/agent src/features/boards src/api` 430/430, `tsc --noEmit`, eslint and prettier clean.
