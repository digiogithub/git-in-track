---
type: comment
item: GIT-US-0053
author: mcp
created: 2026-09-13T21:14:39Z
kind: comment
---

Aligned with the 2026-09-13 client-library decision.

Changed:
- Dependency swapped from `@ag-ui/client` to `@pando-ai/sdk/agui`; vendoring is also ruled out.
- The story now says to use the SDK's `PandoThread`, SSE parser, state/JSON-Patch applier and HITL helpers instead of a home-grown reducer, with local code only for the gaps — and to report in the PR which is which.
- Marked blocked on the browser-safe build of that SDK.
- Added thread-id persistence on the gintrack side, and the interim concurrency rule: one thread per browser tab plus a `BroadcastChannel` guard, until reattach exists. New acceptance criteria for both.
- Dropped the stale `provider.ts:866-1193` range from the prose (the provider file has drifted); the file is still the right seam.

Depends on: PANDO-EP-0001 (browser-safe `@pando-ai/sdk/agui` build, reducer/JSON-Patch/HITL helpers) — hard blocker. PANDO-EP-0003 (thread history, park-on-disconnect, reattach, cancel) — removes the one-thread-per-tab workaround and enables transcript restore on reload.
