---
type: comment
item: GIT-US-0053
author: mcp
created: 2026-09-15T16:45:09Z
kind: comment
---

Closing review: all five child tasks are done and all 12 story criteria verified. SDK pinned, no `@ag-ui/client`, nothing vendored -> web/package.json:41 (`@pando-ai/sdk` 0.2.0); delta +1.06 kB gzipped, recorded on GIT-T-0027 (the PR body itself is the coordinator's to write). Seam in all providers -> web/src/api/provider.ts (eight agent methods, `Capabilities.agent`:1237, `not_supported`), companion/browser/fake implementations, `npx tsc --noEmit` clean and `npm run build` green. SDK-vs-local split -> `PandoThread`, `parseSSE`, `applyJsonPatch` and the HITL helpers used as they ship; local code limited to the provider-backed transport (client.ts, `?repo=` has nowhere to go in `PandoAguiClient.agentUrl()`), `supplementaryReduce` for the three events the SDK's reducer has no branch for, the unapplicable-patch filter, the `AgentMessage` projection, run status, persistence, the tab guard and the server-side thread calls. Ordered interleaving from the fixture -> store.test.ts:106 + threads.test.ts:172-185. Snapshot then deltas, bad patch ignored rather than thrown -> store.test.ts:127/:135, client.test.ts:57-76 (`patchApplies()` filters before `PandoThread.applyStateDelta` can throw). Interrupt parks with pending calls readable -> store.test.ts:157/:195. Resume posts the same thread, a fresh run and trailing `tool` messages, continuing the list -> store.test.ts:171. Thread id persisted per repo in `localStorage`, identity survives a reload -> store.test.ts:90, transcript restored via `reattach()` -> :293. One-thread-per-tab `BroadcastChannel` guard -> threads.test.ts:80-123, store.test.ts:336/:360. Abort ends in `cancelled` with no dangling message -> store.test.ts:243. `RUN_ERROR` and a mid-stream transport failure both terminal -> :207/:218. Fixtures checked in at web/src/features/agent/fixtures.ts; raw frames split mid-frame exercised in companion-provider.test.ts. `npx vitest run src/features/agent/ src/api/agent-provider.test.ts`: 16 files, 158 tests, green.

Two things the earlier comment left open, both resolved or out of scope: the bundle delta is reported here and on GIT-T-0027 (PR body pending, coordinator), and the first live end-to-end exercise against a real adapter remains GIT-T-0118's. The suggested docs/05-web-app.md patch (the `agent` capability and the provider methods in its `DataProvider` section) is still unmade and belongs to whoever owns that file.
