---
type: comment
item: GIT-T-0238
author: claude
created: 2026-09-24T12:13:30Z
kind: comment
---

Review resolved. Reviewer decisions (2026-09-24), now §7 of [[research/2026-09-24-spec-driven-development-overview]] (status: validated):

1. **Granularity**: requirements are blocks inside one `spec` item (`GIT-SP-NNNN`, `docs/.pmngr/specs/`, code `SP`), headed `### GIT-SP-NNNN.R<n> — <title>` with EARS/SHALL + `#### Scenario:` WHEN/THEN. Scoped IDs are permanent, next = max+1 in the file, moving = new ID + `supersedes`. Each requirement behaves as its own unit (status, block rev, trace, verified stamp, list/search/coverage row, MCP get/update with block rev, Pando anchor). Metadata in a front-matter `requirements:` map keyed by `R<n>` (`status`, `trace`, `verified`).
2. **Code anchors**: `// Implements:` / `// Verifies:` markers plus the optional `trace:` entry.
3. **Lifecycle**: reuse the project workflow statuses.
4. **Grammar lint**: warning by default, configurable in project.yaml (raisable to error).
5. **Web coverage matrix**: mandatory in the milestone.
6. **Scope**: E7 CI gate + git hook in; Spec Kit / OpenSpec / Kiro importers deferred to a separate epic with no milestone.
7. **E1**: agent under human supervision, own PR; first story ADR-037 + docs/03 (no code), second story the code, blocked by the first.
8. **Naming**: `spec` / `SP`; link kinds `implements`/`implemented_by`, `modifies`/`modified_by`, `supersedes`/`superseded_by`; targets accept `GIT-SP-NNNN.R<n>` (part of ADR-037).

Created (all `relates_to` this task):

- Milestone **GIT-M-0015** Phase 11 — Spec-driven development
- GIT-EP-0022 Spec data model (GIT-US-0104..0106; ADR story GIT-US-0104)
- GIT-EP-0023 Spec authoring, grammar lint and Spec Delta (GIT-US-0107..0111)
- GIT-EP-0024 Requirement trace engine (GIT-US-0112..0116)
- GIT-EP-0025 Requirement impact analysis over Pando (GIT-US-0117..0120)
- GIT-EP-0026 Spec agent surface in MCP and CLI (GIT-US-0121..0126)
- GIT-EP-0027 Specs in the web app with coverage matrix and impact view (GIT-US-0127..0132)
- GIT-EP-0028 Spec impact CI gate and git hook (GIT-US-0133..0134)
- GIT-EP-0029 Dogfood specs and token benchmark (GIT-US-0135..0137)
- GIT-EP-0030 Spec importers for Spec Kit, OpenSpec and Kiro — no milestone, backlog (GIT-US-0138..0140)

Dependencies are `blocked_by` links. Stories with no blockers are `todo`: GIT-US-0104 (ADR-037), plus three independent prerequisites GIT-US-0112 (gitops ChangedFiles), GIT-US-0117 (Pando wrappers) and GIT-US-0121 (stdio semantic searcher fix). E1 stories carry no `agent-ok`.
