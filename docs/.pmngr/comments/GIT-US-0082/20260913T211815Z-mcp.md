---
type: comment
item: GIT-US-0082
author: mcp
created: 2026-09-13T21:18:15Z
kind: comment
---

Aligned with the 2026-09-13 search decisions and refreshed every drifted line reference.

Changed:
- Stated the settled contract: Pando returns candidates only. A hit is resolved to an item id and every displayed field is re-read from git-in-track's own index; Pando's document metadata is never read. New acceptance criterion.
- Multi-project handling settled as over-fetch at `limit = 20` plus a companion-side path-prefix filter, with the under-return case documented. No structured filtering is asked of Pando. New acceptance criterion.
- Replaced "measure before promising anything at ten thousand" with the measured numbers (~100-200 ms at ~10 000 chunks, does not hold at ~35 000) and a hard fallback above a configured chunk count.
- Line references corrected and verified by grep in /www/git-in-track:
  - `internal/core/query.go:544` -> `:628` (`Index.Search`), `SearchHit` `:518` -> `:602`
  - `internal/server/server.go:411-435` -> `:444` (`handleCapabilities`), `"search": "core"` at `:459`
  - `web/src/api/provider.ts:689` -> `:1179` (`fullTextSearch`), `provider.ts:940` -> `:1493` (`search(query)`)
  - `companion-provider.ts:761` -> `:1022`
  - `internal/server/api.go:75` -> `:79` (`GET /search`)

Depends on: PANDO-EP-0007 (search scale — removes the chunk-count ceiling), PANDO-EP-0005 (REST search surface + a server-side path-prefix filter, replacing the over-fetch workaround). Neither blocks; the story is buildable today.
