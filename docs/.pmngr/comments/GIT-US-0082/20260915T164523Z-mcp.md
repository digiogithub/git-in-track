---
type: comment
item: GIT-US-0082
author: mcp
created: 2026-09-15T16:45:23Z
kind: comment
---

Closing review: left at `in_review`, blocked by one child. GIT-T-0159, GIT-T-0163, GIT-T-0170 and GIT-T-0176 are verified `done`; **GIT-T-0174 blocks the story** — its third criterion ("Vitest cover both values of the mapping") has no test exercising `features.search === 'pando'` through the companion mapping (only `'bleve'` at web/src/api/companion-provider.test.ts:305; the `'pando'` assertions in search-provider.test.ts:68-72 read `FakeProvider`, not `toCapabilities`). Smallest fix: one assertion `toCapabilities({ features: { search: 'pando' } }).fullTextSearch === 'pando'` beside companion-provider.test.ts:2333.

Every other story criterion is met: `Searcher` in internal/core and the WASM build -> internal/core/search.go:29-37, `GOOS=js GOARCH=wasm go build ./wasm` green, no host-only imports in internal/core; native Pando searcher with id resolution -> internal/server/search_pando.go:53-206, `parseCorpusPath` :214-252, unresolvable dropped :105-127; every shown field re-read from this index -> search_pando.go:182-202 (`vault.Item`, `PageByProjectPath`; only score and chunk come from Pando); over-fetch at the cap plus a companion-side prefix filter with the under-return cost documented -> `overFetch` :131-137 (`min(limit*2, 20)`), `corpusPrefix` :139-160 (doc comment states the under-return case), docs/02 §8.1 and docs/21 §6, test `TestSearchOverFetchesAndScopesByPathPrefix`; merged response with origin and score -> internal/server/items.go:525-534, :547-588, test `TestSearchMergesSemanticHitsAfterExactOnes`; fallback with the same shape logged once per state change -> search_settings.go:320-335, tests `TestSearchDegradesWhenPandoFails` / `TestSearchRecoversAfterADegradation` / `TestSearchWithoutPandoIsUnchanged`; `features.search` is `"pando"` only when selected -> server.go:504 + search_settings.go:311-317, and `fullTextSearch` accepts `'pando'` across the providers -> provider.ts:1209, companion-provider.ts:1169, browser-provider.ts:327, fake-provider.ts:1442; docs -> docs/02-architecture.md:694-733, docs/07-cli-and-api.md:1963-1969 and :2799-2824, CHANGELOG.md:49.

Superseded criterion: "the searcher refuses Pando above a configured chunk count" is replaced by the 300 ms latency budget per the recorded decision in the 2026-09-15 comment on this story (the companion cannot observe Pando's chunk count) — implemented at search_pando.go:19-25 and :91-95, with the timeout path covered by `TestSearchDegradesWhenPandoFails/too_slow`. Counted as met.

`go test -race ./internal/core/... ./internal/vault/... ./internal/server/...` passes.
