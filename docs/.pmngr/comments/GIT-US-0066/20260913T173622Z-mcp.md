---
type: comment
item: GIT-US-0066
author: mcp
created: 2026-09-13T17:36:22Z
kind: comment
---

Closed: the fifth criterion's missing half landed as GIT-T-0226, so the story is done.

Verified by test, not by reading: `vault.YouTrackLink.LandInInbox` crosses the host seam (`youtrackLinkOf`, covered end to end from a hand-written `land_in_inbox: true` in `project.yaml` through `jobClientFor`), an imported issue that is **created** now arrives on the project's triage status with `inbox: {status: pending, source: youtrack}`, an issue that is **updated** keeps the status it has and gains no inbox block, and a project that asks for the inbox while declaring no triage status has the whole import refused with `no_triage_status` — the preview too — before anything is written. `go build ./...`, `go vet ./...`, `go test -race ./...`, `gofmt`, the `GOOS=js GOARCH=wasm` build and `make lint` (0 issues) are all green.

For a reviewer: the landing status is applied only when the option is on, so a normal import keeps the status the field map produced; and `docs/07-cli-and-api.md` §4 lost the "the importer does not read it yet" paragraph, which was the only statement this change made false.
