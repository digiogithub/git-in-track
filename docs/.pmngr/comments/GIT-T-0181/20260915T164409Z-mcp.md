---
type: comment
item: GIT-T-0181
author: mcp
created: 2026-09-15T16:44:09Z
kind: comment
---

Closing review, all criteria verified: the widened type compiles across every provider with no `any` -> web/src/api/provider.ts:1345-1375 (`SearchHitSource`, `SearchHit.source`/`snippet`, `SearchResult`) and :1745 (`search(query): Promise<SearchResult>`), `npx tsc --noEmit` clean (strict, no `any` added); the companion maps origin and snippet -> companion-provider.ts:963-999 (`toSearchHits` reads `snippet`/`source`, unknown reads as `'core'`; `toSearchResult` reads the `{hits, degraded}` envelope and the legacy `results`/bare-array shapes), browser-provider.ts:664-671 stamps `'core'`; the fake yields both kinds -> fake-provider.ts:1370-1372, :1438-1439, :1885-1918. Tests: `npx vitest run src/api/search-provider.test.ts src/api/companion-provider.test.ts src/api/browser-provider.test.ts` passes (envelope, legacy shapes, unknown origin, scripted semantic hits, degraded flag).
