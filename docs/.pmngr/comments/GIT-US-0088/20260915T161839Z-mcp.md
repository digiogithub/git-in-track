---
type: comment
item: GIT-US-0088
author: mcp
created: 2026-09-15T16:18:39Z
kind: comment
---

Wave-3 implementation landed on `feat/phase-9-agent-and-search` (uncommitted at the time of writing; the coordinator commits).

Met: `search.semantic` in the vault dispatch table with all the logic (GIT-T-0192, verified from wave 2); `search_semantic` in `tools/list` on a read-only server, returning hits with kind, id/path, title, status, project, score, snippet and the current `rev`, plus `engine` (and `degraded` when the backend reports it) — GIT-T-0194; typed `unavailable` refusal with a `retry` naming `search_items`, never a silent fallback; the untrusted-content note (added by `register`, asserted by `TestUntrustedContentIsMarked`); surface-pinning tests and counts in `internal/mcp/tools_test.go`, `cmd/gintrack/mcp_test.go`, `internal/server/mcp_test.go`, the `gintrack mcp` long help and `AGENTS.md` (8 read-only, 23 total); behaviour tests through the in-memory client harness for a hit, an empty result and the unavailable backend (`internal/mcp/tools_search_test.go`); the four-row routing table with one example per row in the generated skill and in `docs/08-mcp-server.md` §4.19, with the "when to use which search" note.

Open (files owned by other waves; exact patches are in the comments of GIT-T-0196 and GIT-T-0198): `docs/07-cli-and-api.md` §4.9 still says "twenty-two tools: seven read-only" and its `--list-tools` sample lacks `search_semantic`; `docs/20-agent-interface.md` §5 does not yet mirror the routing table.

Verified: `go build ./...`, `go vet` and `go test -race` on `./internal/mcp/...`, `./cmd/gintrack/...`, `./internal/server/...` all pass; `golangci-lint v2.13.2` clean on `./internal/mcp/... ./cmd/gintrack/...`.
