---
type: comment
item: GIT-EP-0019
author: mcp
created: 2026-09-13T21:16:58Z
kind: comment
---

Aligned with the 2026-09-13 corpus-export decision and the KB fidelity findings.

Changed:
- Sync path is now `KBAutoImport = true` with `KBWatch = false`, companion-driven re-sync, instead of `KBPath` + `KBWatch`.
- Added the settled search contract: Pando returns candidates only; the companion resolves hits to item ids and re-reads authoritative fields from its own index. No structured filtering is required of Pando. New criterion for it.
- The metadata round-trip is recorded as a Note and an explicitly non-blocking limitation (report A1/A2/A5/A7), not a gate — the design deliberately does not depend on reading structured front matter back out of Pando.
- Added the transport seam note (MCP now, REST later behind `internal/pando.Client`) and the embedding-model pin being global to the instance.
- Added a Pando dependencies paragraph with plain ids.

Depends on: PANDO-EP-0005 (KB metadata fidelity + REST search/reindex), PANDO-EP-0006 (MCP HTTP auth), PANDO-EP-0007 (search scale). Everything else is buildable today.
