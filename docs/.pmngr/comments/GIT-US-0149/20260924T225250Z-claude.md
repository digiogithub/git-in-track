---
item: GIT-US-0149
author: claude
created: 2026-09-24T22:52:50Z
type: comment
---

Implemented in the jj change described `feat(server): list branches and recent commits for impact pickers`.

- `gitops.Backend.Branches` on go-git, system git and jj (bookmarks as branches, remote bookmarks as `origin/main`, the `git` pseudo-remote and `origin/HEAD` left out, no jj operation written), with table tests per backend.
- `GET /api/v1/git/refs?repo=<id>&limit=<n>` in the existing `/git` subtree: branches plus the last N commits from `Commits`; `503 unavailable` without git history. No auth or path-validation change.
- Web: `listGitRefs` on the providers (browser-only rejects with `unavailable`), `useGitRefs`, and the impact view's Base/Head datalists now offer branches and labelled recent commits, still free text, falling back to the sync-status suggestions.
- Docs: 05, 06 §7.5, 07, CHANGELOG. GIT-US-0131's first criterion ticked.

Verified: make lint, make test, make wasm, vite build.
