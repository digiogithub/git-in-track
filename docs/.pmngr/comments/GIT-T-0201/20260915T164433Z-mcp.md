---
type: comment
item: GIT-T-0201
author: mcp
created: 2026-09-15T16:44:33Z
kind: comment
---

Closing-pass verification, all four criteria met. GET returns all seven fields -> internal/server/search_settings.go:343-372 (`backend`, `mcpUrl`, `restUrl`, `projectId`, resolved `corpusDir`, live `reachable`/`reachableError` probe, `lastExport`, `documents`) rendered by `view` :384-429, handler :524-526; PATCH persists and returns `persisted` -> `persist` :498-517 reloads with config.Load, replaces only the five non-secret `search.pando` fields, config.Save, returns true, and the handler :529-546 adopts in process first (`apply` :447-488 -> `rebuild`) — same shape as gitState.persist. No ConfigPath -> `persisted:false` with the process change kept -> :499-501, asserted by TestSearchSettingsPatchWithoutAConfigFile. Invalid values -> `invalid_request` 400 with nothing adopted -> :466-480 (URL rule delegated to pando.New so it cannot drift from the client; relative corpusDir rejected at :477-479), handler `failProblem(codeInvalidRequest)` :534. `go test -race ./internal/server/` passes: TestSearchSettingsReportsTheCorpusAndTheBackend, TestSearchSettingsPatchPersists, TestSearchSettingsPatchWithoutAConfigFile, TestSearchSettingsPatchRejectsBadValues. Confirmed no token is patchable or reported (searchSettingsPatch :437-443 has no token field; the view carries none).
