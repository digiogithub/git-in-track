---
type: comment
item: GIT-US-0059
author: mcp
created: 2026-09-13T16:21:29Z
kind: comment
---

Closed: the last open task, GIT-T-0111, landed the documentation in `docs/05-web-app.md` §3.1 (the dialog, its five controls, the preview-versus-run distinction and the per-issue summary) and `docs/02-architecture.md` §2 (why the feature is companion-only and why two capability flags, not one).

Reviewers should know that two statements circulating in the task notes were wrong and are not repeated in the docs: `POST /api/v1/youtrack/import` **always** queues and answers `202` with a job id — the dialog's inline-result branch is dead code no runtime reaches today — and the options panel has five controls, not the six its own doc comment claims. `CHANGELOG.md` was left to the agent that owns it this wave.
