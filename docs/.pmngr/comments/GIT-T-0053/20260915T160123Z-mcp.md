---
type: comment
item: GIT-T-0053
author: mcp
created: 2026-09-15T16:01:23Z
kind: comment
---

Done. `ui/ToolCallCard.tsx` and `ui/ReasoningBlock.tsx`, both native `<details>` — no Radix package added. Card: name, status badge (Calling / Waiting / Done over `badge.tsx` tones), collapsed body with pretty-printed arguments (`JSON.stringify(args, null, 2)`, falling back to the raw stream while the JSON is still partial) and the result, both escaped `<pre>`. Results clamp at `RESULT_CLAMP = 4096` with a "Show more (n kB total)" / "Show less" toggle. Reasoning is collapsed, monospace and muted, and never rendered as Markdown.

Tests cover the three statuses, keyboard focus and activation of the disclosure, the escaped arguments, the partial-args fallback, the clamp and its expansion, and the absent result section. `node web/scripts/check-design-tokens.mjs` passes (62 assertions); only existing tokens and `components/ui/` primitives are used.
