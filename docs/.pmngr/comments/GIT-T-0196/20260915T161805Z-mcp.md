---
type: comment
item: GIT-T-0196
author: mcp
created: 2026-09-15T16:18:05Z
kind: comment
---

Counts updated everywhere I own; **docs/07 is not mine in this wave**, so this stays in_progress for that one criterion.

Done (7 read / 22 total → 8 read / 23 total):
- `internal/mcp/tools_test.go` — `search_semantic` added to the pinned `readTools`.
- `cmd/gintrack/mcp_test.go` — added to the read-only `--list-tools` expectation; `tools/list` count 22 → 23.
- `internal/server/mcp_test.go` — `mcpTools` capability length and the streamable-HTTP `tools/list` count 22 → 23, and the "twenty-two" wording.
- `cmd/gintrack/mcp.go` long help — "twenty-three … with writes enabled, eight without", the eight read-only tools listed, plus a sentence saying `search_semantic` needs the Pando backend and refuses instead of falling back.
- `AGENTS.md` — "(8 tools read-only, 23 with `--allow-write`)", the twenty-three-tool list, and a short paragraph on when to reach for it.
- `docs/08-mcp-server.md` — header, §3 principle 8, §4 intro counts, catalog table row, new §4.19 `search_semantic` (old §4.19 Planned tools → §4.20).

Not done, needs whoever owns docs/07 (`docs/07-cli-and-api.md:1183-1187`), which still carries the pre-existing drift:

> There are **twenty-two tools**: seven read-only — `list_items`, `search_items`, `get_item`, `list_inbox`, `list_kb_pages`, `get_kb_page` and `search_kb` — and fifteen writes.

becomes

> There are **twenty-three tools**: eight read-only — `list_items`, `search_items`, `search_semantic`, `get_item`, `list_inbox`, `list_kb_pages`, `get_kb_page` and `search_kb` — and fifteen writes.

and the `--list-tools` sample output just above it (docs/07:1155-1180) needs `search_semantic` inserted alphabetically between `push_comment_to_youtrack` and `search_items`… i.e. after `search_kb`, and the startup line `…, 7 tools (read-only)` becomes `8 tools (read-only)`. (The story's note about "twelve tools / six write tools" no longer matches the file: the list there is already complete, only the seven/twenty-two counts are stale.)
