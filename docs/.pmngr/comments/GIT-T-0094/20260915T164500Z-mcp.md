---
type: comment
item: GIT-T-0094
author: mcp
created: 2026-09-15T16:45:00Z
kind: comment
---

Closing review, all four criteria verified: cards render inline with title, type, status and priority -> `web/src/features/agent/ui/ItemCards.tsx:37-58` reusing `features/backlog/Badges.tsx`, mounted through the `renderToolResult` seam (`ui/MessageList.tsx:35,95`, `ui/MessageBubble.tsx:37,163`, wired at `ui/AgentPage.tsx:307-309`), test `ui/ItemCards.test.tsx:26`; cache reuse with no per-card request -> the executor resolves via `ToolContext.lookup.item` (`tools/backlog.ts:206-227`) which the page binds to `queryClient.ensureQueryData(backlogKeys.detail(...))` (`ui/AgentPage.tsx:106-118`), ids de-duplicated at `backlog.ts:211` and asserted in `tools/registry.test.ts:261-281`, cards then render from the stored tool result rather than a fresh query (`ItemCards.tsx:31`); unresolved ids reported to the agent and shown muted -> `backlog.ts:222-226` plus `ItemCards.tsx:60-65`, test `ItemCards.test.tsx:49`; vitest -> `npx vitest run src/features/agent/ui/ItemCards.test.tsx src/features/agent/tools/registry.test.ts` green.
