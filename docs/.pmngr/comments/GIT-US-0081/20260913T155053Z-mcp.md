---
type: comment
item: GIT-US-0081
author: mcp
created: 2026-09-13T15:50:53Z
kind: comment
---

Landed: `SyncEngineCard` (knobs + live queue table with per-row retry/cancel) in Settings, `useSyncJobEvents`/`useSyncJobs`/`useRetrySyncJob`/`useCancelSyncJob` in `web/src/features/sync/queries.ts`, app-wide job toasts in `SyncJobToasts` mounted in `AppShell`, and the four job methods plus the extended settings pair on `DataProvider` and all three providers.

A reviewer must know two things. The card is gated on `SyncSettings.engine` being present rather than on a capability flag — `GET /capabilities` declares no engine feature — and `AppShell` now hosts a single app-wide `ToastProvider`, which is also what finally gives the Settings page toasts at all (it had none; every card's `useToast` was a silent no-op there). Verified: `npm run lint`, `npm run typecheck`, `npm test` (727 passing), `npm run tokens:check`, `npm run build`, `make lint-web` all clean.
