---
type: comment
item: GIT-US-0077
author: mcp
created: 2026-09-13T21:18:49Z
kind: comment
---

Confirmed buildable today and strengthened the security criterion.

Changed:
- Records that go-sdk v1.4.1's streamable client is verified against Pando's hand-rolled `/mcp` (405s on GET and DELETE included) — no shim, no upstream change needed to start.
- The non-loopback refusal is now non-overridable in v1: the previous "unless the operator opts in explicitly" escape hatch is removed, because the surface is not just unauthenticated but CORS `*` with global auto-approve.
- Added the honest caveat, as a criterion: loopback binding is not a boundary against the user's own browser while that CORS policy stands; it must be said in the doc comment and in docs/07.
- Made the package an explicit transport seam — no MCP type in the exported surface — so the later REST switch is a drop-in. New criterion.
- Added session reuse rationale (Pando's session map has no eviction) and refreshed the `kb_search_documents` references to `:227-259` / `:274-279` / `:296-316`, noting the result already carries `metadata`.

Depends on: PANDO-EP-0006 (authenticate the MCP HTTP transport), PANDO-EP-0005 (REST search surface, lets this drop MCP). Neither blocks — this is the most solid story in the epic and can start now.
