---
type: comment
item: GIT-US-0077
author: mcp
created: 2026-09-15T17:01:16Z
kind: comment
---

Done: the last open child, GIT-T-0142, is closed. An empty `search.pando.projectId` now resolves to `pando.SanitizeProjectID(<mount path>)` in `searchState.projectID()`, which is what the constructed `*pando.Client` carries and therefore what `SearchCode` falls back to — the derived default docs/07:298 and `config.SearchPando` had been promising without anyone deriving it. Covered by a table test over sample paths and an end-to-end settings test. All four children (GIT-T-0142, 0148, 0152, 0156) are done; `go build ./...`, `go vet`, `go test -race ./internal/server/... -count=1` and `golangci-lint v2.13.2` (0 issues) green.
