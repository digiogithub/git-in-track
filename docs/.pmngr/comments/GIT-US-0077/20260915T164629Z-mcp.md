---
type: comment
item: GIT-US-0077
author: mcp
created: 2026-09-15T16:46:29Z
kind: comment
---

Closing review. Status unchanged (`in_review`): every criterion of the story itself checks out, but the child GIT-T-0142 is not `done`, so the story does not close yet. Nothing is ticked.

Evidence, criterion -> proof: streamable-HTTP session over the MCP Go SDK reused across calls -> internal/pando/client.go (lazy `acquire` under c.mu, StreamableClientTransport with DisableStandaloneSSE) + TestSessionIsLazyAndShared. No MCP type in the exported surface -> `go doc -all ./internal/pando` shows no mcp.* type; the envelope `toolResult` is unexported (search.go:26-52). SearchKB parses {filePath, chunk, score, rank, tags, updated, metadata} -> search.go + types.go:28-49 + TestSearchKBParsesRecordedPayload. SearchCode parses code_hybrid_search (via structuredContent.metadata) and ListProjects parses code_list_projects -> search.go:197-305 + TestSearchCodeParsesStructuredMetadata, TestListProjectsParsesRecordedPayload. Configurable timeout and typed errors for timeout / transport failure / tool error -> `classify` and errors.go + TestTimeoutIsDistinctFromUnreachable, TestToolErrorIsDistinctFromEmptyResult. Health bounded, dropped session reconnects -> TestHealthUnreachable, TestHealthReconnectsAfterRestart, TestSessionReconnectsAfterRestart. `config.Search.Pando{url, project_id}` parses, validates and refuses a non-loopback URL with no override -> internal/config/config.go:466-503, validate.go:192-228 and internal/pando/client.go:310-324 + TestValidatePandoRefusals, TestValidatePandoAllowRemote, TestNewRefusesNonLoopbackURL, TestNewAllowRemoteIsTheOnlyOverride. The browser caveat is stated in both places -> internal/pando/doc.go:60-67 and docs/07-cli-and-api.md:396-405 (this closes the one criterion the implementation comment could not). No internal/core edge in either direction -> `go list -deps ./internal/pando` and no importer under internal/core. `go test -race ./internal/pando/... ./internal/config/...` green.

Blocking child: **GIT-T-0142**. Its "derived default project_id" criterion fails — `pando.SanitizeProjectID` has no caller, an empty `projectId` yields `ErrNotConfigured` from SearchCode (internal/pando/search.go:198-203) while docs/07-cli-and-api.md:298 and internal/config/config.go:490-491 promise a derived default. Details and the smallest fix are in that task's comment. The story can close as soon as it does.
