---
type: comment
item: GIT-US-0057
author: mcp
created: 2026-09-15T17:01:23Z
kind: comment
---

Done: both open children are closed. GIT-T-0050 gained the missing scroll test — "jump to latest" appears once the reader scrolls away from the bottom and clears on click, with the jsdom layout numbers stubbed. GIT-T-0062 needed a production change: deleting the open conversation always started a blank one instead of selecting a neighbour; `deleteThread` now selects the row that took its place, else the one above, and only falls back to `newThread()` when nothing survives — asserted in `store.test.ts` (three cases) and end to end in `AgentPage.test.tsx`. All five children (GIT-T-0047, 0050, 0053, 0058, 0062) are done; `npx vitest run src/features/agent src/features/boards src/api` 430/430, `tsc --noEmit`, eslint and prettier clean.
