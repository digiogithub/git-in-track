---
type: comment
item: GIT-US-0057
author: mcp
created: 2026-09-15T16:45:05Z
kind: comment
---

Closing-pass verification (read-only review of HEAD 62f03ec). Status left at `in_review`: the story's own nine criteria are met, but **two children are not `done`** — GIT-T-0050 (no vitest coverage of the scroll behaviour) and GIT-T-0062 (deleting the active thread opens a new empty one instead of selecting a neighbour, `store.ts:532`). See those two comments for the smallest fixes. GIT-T-0047, GIT-T-0053 and GIT-T-0058 are `done`.

Story criteria: `/agent` gated on `capabilities.agent` in router and sidebar -> `app/router.tsx:170-173`, `app/layout/AppShell.tsx:64-66,118`, `ui/AgentPage.tsx:67-71`, tests `router.test.ts:12`, `AppShell.test.tsx:140,147`; Markdown through `web/src/markdown/`'s public surface with sanitisation last and raw HTML inert -> `ui/MessageBubble.tsx:78-95`, test `MessageBubble.test.tsx:24`; visible streaming plus bottom-pinned auto-scroll -> `MessageBubble.tsx:49-104` and `ui/MessageList.tsx:24,47-68`; collapsible tool-call cards and collapsed reasoning over native `<details>` -> `ui/ToolCallCard.tsx`, `ui/ReasoningBlock.tsx`, tests `ToolCallCard.test.tsx:21,33,85`; Stop cancels and settles as cancelled with the partial message kept -> `store.ts:456-467` (status `cancelled`, messages untouched), tests `store.test.ts:243` and `AgentPage.test.tsx:100`; thread switching restores the transcript and the list survives a reload -> `store.ts:390-420`, `ui/threadMeta.ts:33` (`gintrack:agent:threads`), tests `AgentPage.test.tsx:140,189`, `store.test.ts:263,293`; only `components/ui/` primitives and token aliases -> `node web/scripts/check-design-tokens.mjs`, 62 assertions pass; keyboard navigable with a polite live region -> `MessageList.tsx:80-85`, test `MessageBubble.test.tsx:88`; vitest covers streaming, stop, thread switching and the capability gate -> the tests named above.

Re-run here: `npx vitest run src/features/agent src/app/router.test.ts src/app/layout/AppShell.test.tsx` — 17 files / 162 tests green.
