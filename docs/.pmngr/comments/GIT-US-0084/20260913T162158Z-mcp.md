---
type: comment
item: GIT-US-0084
author: mcp
created: 2026-09-13T16:21:58Z
kind: comment
---

Last open task closed (GIT-T-0184): `docs/02-architecture.md` now carries the engine's place among the companion's background components. There was no existing watcher/committer/tunnel list to append to, so the paragraph went under the §3.2 companion-mode table together with a "Background jobs" row — built by `server.New`, started in `Server.Start`, closed on the same path with `context.WithoutCancel`, draining for a bounded five-second grace period and journalling the rest, handlers registered before `Start` because `Start` replays the journal, idle when no integration is configured.

While there, two claims in §6 that waves 3 and 4 made false were corrected: `internal/syncengine` said "not reachable from any surface yet" and `internal/youtrack` said "nothing imports it yet". Both now name their callers and the four registered job kinds. `CHANGELOG.md` carries the same two stale sentences and belongs to another agent — worth a pass.
