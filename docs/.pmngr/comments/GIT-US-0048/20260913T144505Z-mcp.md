---
type: comment
item: GIT-US-0048
author: mcp
created: 2026-09-13T14:45:05Z
kind: comment
---

Landed: the connection splits in two. The committed half is `integrations.youtrack` in `project.yaml`, read and written by `internal/config/projectlink.go` through an in-place YAML-node edit that keeps comments, key order and every unmodelled section; the secret half is `Config.Integrations.YouTrack[<projectKey>].token` in the existing `0600` file, with a `GINTRACK_YOUTRACK_TOKEN` override and a `flag > env > file > none` provenance a surface can report without rendering the value. `features.youtrack` and `features.youtrackSupported` are on `/api/v1/capabilities`. ADR-032 is written and the "stores no credentials" promise in `docs/10-development-guidelines.md` now says which single credential is stored, where, and what `0600` does and does not protect.

Two things a reviewer must know. First, the `project.yaml` half lives in `internal/config`, not `internal/core`: that package was held by another agent for the whole wave. Nothing is lost — core drops unknown keys and never re-serializes the file — but the typed `ProjectConfig.Integrations` mirror and the `E-PROJ-INTEGRATION` diagnostic are still owed, and `config.YouTrackLink.Validate` is the validation they should call. Second, `config.Validate` cannot reject "a token for an unknown project key", because the configuration package holds no inventory of project keys; it checks the key's shape and a non-empty token instead. The only unticked box is the `CHANGELOG.md` entry of GIT-T-0032 — that file belonged to another agent this wave; the suggested line is in the task's notes.
