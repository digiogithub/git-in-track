---
type: comment
item: GIT-T-0205
author: mcp
created: 2026-09-15T16:45:00Z
kind: comment
---

Closing-pass verification, all four criteria met. Renders status, saves, reports `persisted` -> web/src/features/settings/PandoSearchCard.tsx (plain useState :134-148 + useEffect load + imperative save :212-218, no react-hook-form and no zod), mounted at SettingsPage.tsx:120; tests "renders the settings document: backend, endpoints, probe and corpus", "saves the five location fields and says the change reached the file", "says when a change only reached the running process". Reindex progress and a clear unreachable message -> :226-243 and the `search.progress`-driven progress block :466-476 with the finished job and its kb counts :477-490, plus `searchSettingsMessage` :76-85 mapping `search_reindex_running`/`search_not_configured` to their own sentences; tests "follows a reindex from the event hub through to the finished job", "explains a reindex that is already running…", "reports a Pando that did not answer, with what it said". Embedding-model warning visible and instance-global -> :515-519, test "keeps the embedding-model pin visible". Gate -> :127 `if (!provider?.capabilities.searchSettings) return null`, false in browser-provider.ts:333 and in the fake default, test "is absent on a runtime with no search settings, browser-only mode included". `npx vitest run src/features/settings/PandoSearchCard.test.tsx src/api` -> 209 passed (10 in the card file). Note for the record: the companion always reports `searchSettings: true` (companion-provider.ts:240) because the server sends no such feature flag — the client honours one if it ever appears (companion-provider.ts:1197). That is the deliberate "the card exists before anything is configured" choice, not a gap.
