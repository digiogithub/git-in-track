---
type: comment
item: GIT-T-0027
author: mcp
created: 2026-09-15T16:44:04Z
kind: comment
---

Closing review, all 3 criteria verified. Pinned exactly -> `"@pando-ai/sdk": "0.2.0"` (web/package.json:41), no `@ag-ui/client` anywhere; `npm run build` re-run here and it succeeds with no Node polyfill and no warning beyond the pre-existing 500 kB chunk notice, size delta recorded in the 2026-09-15 comment (+1.06 kB gzipped). Factory + abort -> `createAgentTransport` (web/src/features/agent/client.ts:77+) runs through `DataProvider.runAgent`, which POSTs to `/api/v1/agent/run?repo=` with the companion bearer header and an `AbortSignal`; `client.test.ts:78` asserts the repo and the signal reach the provider, `store.test.ts:243` asserts an in-flight abort. Vitest: 16 files / 158 tests green over `src/features/agent/` + `src/api/agent-provider.test.ts`. Superseded criterion: "the factory produces a `PandoAguiClient`" — replaced by the transport-is-the-provider decision recorded on this task on 2026-09-15, because `PandoAguiClient.agentUrl()` leaves no room for `?repo=` and a second `fetch` owner would bypass the provider's RFC 7807 mapping; the header/abort behaviour the criterion cares about is implemented and tested. The bundle-size line still has to reach the PR body, which is the coordinator's.
