---
type: comment
item: GIT-T-0034
author: mcp
created: 2026-09-15T16:44:17Z
kind: comment
---

Closing review, all 4 criteria verified. Superseded: "create `events.ts`, a local reducer" -> the 2026-09-13 realignment makes the SDK's `PandoThread` the reducer and forbids a parallel one; the replacement is web/src/features/agent/threads.ts, which composes it and adds only the gaps (`supplementaryReduce` for `MESSAGES_SNAPSHOT`/`TEXT_MESSAGE_CHUNK`/`ACTIVITY_SNAPSHOT`, the `AgentMessage` projection, hydrate/reattach). Ordered replay with interleaved text, reasoning and tool calls -> store.test.ts:106 over the `simpleTurn` fixture, plus threads.test.ts:172-185 on the projection (reasoning kept out of the visible reply, tool results resolved). Snapshot then deltas -> store.test.ts:127; a bad patch dropped without corrupting state -> store.test.ts:135 and client.test.ts:57-76, where `patchApplies()` filters the event before `PandoThread.applyStateDelta` can throw (the SDK throws; the criterion says ignore, so the filter is the local gap). Unknown types and out-of-order ends tolerated -> threads.test.ts:162 and store.test.ts:147. Fixtures are checked in under the feature folder at web/src/features/agent/fixtures.ts; raw `data:` frames split mid-frame are exercised where the parser lives, in companion-provider.test.ts. Vitest green.
