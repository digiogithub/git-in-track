---
type: comment
item: GIT-T-0021
author: mcp
created: 2026-09-15T15:29:38Z
kind: comment
---

docs/07 done; left **in_progress** for the CHANGELOG criterion only.

`docs/07-cli-and-api.md` now carries: the `agent:` and `search.pando:` stanzas in the §3.2 schema with a comment per key; the three `GINTRACK_PANDO_*` rows in the §3.3 environment table plus a paragraph on why they are separate names and how `token` / `tokenFile` / the row override resolve; `--agent` in the §4.1 flag list; `features.agent` in the §5.5 capabilities example with the "both halves" note and the companion-only statement; and a new §5.5 subsection **"The agent proxy (GIT-US-0049)"** covering all eight routes, the one-to-one mapping and its two exceptions (`/health` → `{path}/healthz` unauthenticated, `/run` → `{path}/{agent}`), `?repo=` routing and its typed 404, the SSE framing (bare `data: {json}`, discriminated by the JSON `type`), the header hygiene, the `/info` rewrite with an example document, the `maxRuns` refusal with `Retry-After`, and a table of the six problem codes.

Remaining: `CHANGELOG.md` has no entry, because that file is outside this wave's ownership. Suggested addition under `## [Unreleased]` (replacing "Nothing yet."):

```markdown
### Added

- **An agent proxy to a local Pando AG-UI adapter** (`GIT-US-0049`, docs/07 §5.5).
  `gintrack serve --agent` mounts `/api/v1/agent`, which relays AG-UI runs, threads and
  cancellations to a `pando agui-serve` process configured under `agent.pando`. The
  browser holds only the companion's own bearer token: the Pando credential is injected
  server-side, the browser `Origin` is stripped, and the discovery document is rewritten
  so no response names the Pando origin. Runs stream unbuffered, a disconnect cancels the
  upstream run, and `agent.pando.maxRuns` caps the runs in flight with a `503` and a
  `Retry-After`. `GINTRACK_PANDO_TOKEN` overrides the configured token; `features.agent`
  reports the capability.
```
