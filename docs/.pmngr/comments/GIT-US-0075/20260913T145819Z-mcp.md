---
type: comment
item: GIT-US-0075
author: mcp
created: 2026-09-13T14:58:19Z
kind: comment
---

Wired the two dangling core helpers into the vault: `checkSprintDates` now enforces both-or-neither and exempts drafts (`IsDraft`), refusing a dated collision with `core.SprintOverlapMessage`, and `Workspace.Sprints` takes `status []core.SprintStatus`, filtering with `core.FilterSprintsByStatus` and ordering with `core.SortSprintsForListing`.

Reviewer note: the REST query-string half of GIT-T-0107 (`GET /api/v1/sprints?status=current,upcoming` in `internal/server/sprints.go`) is left to the server agent — it only has to split on commas and forward the values as `status`. Tests: `TestWorkspaceSprintDrafts` and `TestWorkspaceSprintListByDerivedStatus` in `internal/vault/sprint_test.go`.
