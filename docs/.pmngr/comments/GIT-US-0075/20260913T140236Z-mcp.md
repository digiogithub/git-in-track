---
type: comment
item: GIT-US-0075
author: mcp
created: 2026-09-13T14:02:36Z
kind: comment
---

The `internal/core` half is complete and ADR-034 is written; the `internal/vault` / `internal/server` wiring and `docs/04` §8.2/§8.4 are not, because those files belonged to other agents this wave.

**Done (GIT-T-0096, GIT-T-0101):** `core.SprintStatus` (`draft|upcoming|current|completed`) with a pure `(*Sprint).DerivedStatus(now)` that reads no clock and resolves no timezone; `state: closed` always derives `completed`, dates or not. Dates are now both-or-neither — a dateless sprint validates clean as a draft, one date alone or an inverted range stays `E-SPRINT-DATES`. `IsDraft`, `TotalDays`, `RemainingDays` and `Overlaps` all behave for drafts, pinned by tests. `SprintSummary.Status` carries the derived value and nothing writes it to a file. Seams for the two wiring tasks: `ParseSprintStatus`, `FilterSprintsByStatus`, `SortSprintsForListing`, `SprintOverlapMessage` — see the comments on GIT-T-0103 and GIT-T-0107.

**Left:** GIT-T-0103 (`checkSprintDates` in `internal/vault/sprint.go`), GIT-T-0107 (`SprintListParams` + `GET /api/v1/sprints`), and the `docs/04` half of GIT-T-0113. Acceptance criteria 3, 5 and 6 are therefore still open; the rest hold.

**Reviewer note.** The one judgement call: a *closed* sprint with no dates derives `completed`, not `draft` — the story says "`state: closed` always derives `completed`", which outranks the task's ordering. Nothing else deviates.
