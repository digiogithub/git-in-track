---
type: comment
item: GIT-T-0159
author: mcp
created: 2026-09-15T16:05:47Z
kind: comment
---

Landed. `internal/core/search.go`: `Searcher` with the existing `Search(q string, limit int) []SearchHit` signature, `SearchSourceCore`/`SearchSourcePando`, and `var _ Searcher = (*Index)(nil)` so a drift in the signature breaks the build here rather than in the companion. `SearchHit` gained `Source string \`json:"source,omitempty"\``, set to `"core"` by `Index.Search` for item and page hits alike, so existing callers get the core backend without opting in. No `net/http` and nothing else WASM-hostile; `GOOS=js GOARCH=wasm go build ./wasm` builds and `go test -race ./internal/core/...` passes. New test `TestIndexSatisfiesSearcher`.
