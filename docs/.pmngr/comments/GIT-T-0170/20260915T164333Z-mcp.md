---
type: comment
item: GIT-T-0170
author: mcp
created: 2026-09-15T16:43:33Z
kind: comment
---

Closing review, all criteria verified: exact hits lead in their existing order then deduplicated semantic ones -> internal/server/items.go:547-577 (`mergeSemantic` concatenates, `searchHitKey` at :583-588 dedups by item id or vault+path), test `TestSearchMergesSemanticHitsAfterExactOnes`; the two score spaces are never compared -> no sort of the merged slice anywhere in items.go:560-577; a Pando failure returns the core result in the same envelope -> items.go:556-559 plus the unchanged `{query,hits,total,engine,degraded}` write at :530-534, tests `TestSearchDegradesWhenPandoFails` (unreachable/unauthorized/timeout/switched off) and `TestSearchWithoutPandoIsUnchanged`; logged once per state change -> internal/server/search_settings.go:320-335 (`noteDegraded`, CompareAndSwap both ways, `ErrNotConfigured` exempt), test `TestSearchRecoversAfterADegradation`. `go test -race ./internal/server/...` passes.
