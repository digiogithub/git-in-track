---
type: comment
item: GIT-T-0118
author: mcp
created: 2026-09-16T15:58:07Z
kind: comment
---

**Live re-verification with the PANDO-US-0031 fix — the chain now completes end to end with the MCP gateway OFF. All steps pass.**

Run 2026-09-16, fully isolated: gintrack built from tag **v1.6.0** (not the working tree), Pando built with the fix, **v0.715.7-0.20260916155247-38abda0138cd**. Temp copy of this repository, temp `GINTRACK_CONFIG`, ports 18317 (companion) / 18090 (agui-serve). The user's companion on :7317, `~/.config/gintrack/`, `~/.config/pando/` and this repository's own `.pando.toml` were not touched.

## Commands

```
GINTRACK_CONFIG=<tmp>/state/config.yaml GINTRACK_TOKEN=<token> \
  gintrack add <tmp>/repo --docs docs                       # repo id "repo", 359 items
  gintrack agent init --repo <tmp>/repo --companion-url http://127.0.0.1:18317 \
                      --agui-port 18090 --pando <pando-fixed> --json
pando agui-serve --cwd <tmp>/repo --port 18090 --no-tls --token-file <tmp>/state/agui/repo.token
gintrack serve --agent --mcp-http --port 18317 --no-open
```
with `agent.enabled: true`, `agent.pando.url: http://127.0.0.1:18090`, `agent.pando.tokenFile: …/repo.token`, `mcp.enabled: true`.

## Step results

1. **`agent init` — pass.** Wrote `.pando.toml`, `agents/personas/backlog-assistant.md`, `agents/skills/gintrack-search/SKILL.md`; `"tokenEncrypted": true`. The generated file carries exactly what the fix needs: `[ToolDiscovery] Enabled = false / Mode = 'off'`, `[MCPGateway] Enabled = false`, `[AGUI] FrontendTools = true / HumanInTheLoop = true / AutoApprove = false / RequireToken = true / MaxConcurrentRuns = 4`, `Tools = ['gintrack_*', 'kb_search_documents', 'kb_get_document', 'code_hybrid_search', 'code_find_symbol']`, `[MCPServers.gintrack]` `Type = 'streamable-http'` → `http://127.0.0.1:18317/mcp`, and `[MCPServers.gintrack.Auth]` `Type = 'bearer'` with an `age1:`-prefixed encrypted token (three `age1` occurrences, none printed anywhere).
2. **agui-serve — pass.** Banner `Persona: backlog-assistant`, no unknown-persona warning. `/healthz` → 200 `{"status":"ok","version":"v0.715.7-…","maxConcurrentRuns":4,"draining":false}`.
3. **Companion — pass.** Banner `mcp: http://127.0.0.1:18317/mcp (read-only)`, `agent: http://127.0.0.1:18090/api/v1/agui (backlog-assistant)`; corpus exported, 359 items + 61 pages, 420 files. `/api/v1/agent/health` → 200 (Pando's healthz relayed). `/api/v1/agent/info` → 200, agents `coder` and `backlog-assistant`, model `copilot.gpt-5.6-luna`, capabilities `frontendTools/humanInTheLoop/interrupts/sharedState` all true, and **no `18090` anywhere in the body**.

4. **Chat turn with `AutoApprove = false` (what the template writes) — PASS, the interrupt/resume cycle works.** `POST /api/v1/agent/run?repo=repo` with `curl -N`, question *"How many stories does project GIT have in status done? Use the gintrack tools and answer with the number."*

   First leg (`threadId=t1`, `runId=r1`), event sequence in order:
   ```
   RUN_STARTED, STATE_SNAPSHOT, CUSTOM×2,
   TOOL_CALL_START toolCallName="gintrack_list_items", TOOL_CALL_ARGS, TOOL_CALL_END, STATE_DELTA,
   TOOL_CALL_START toolCallName="pando_permission_request" (toolCallId "perm-dfc588fa-…"),
   TOOL_CALL_ARGS, TOOL_CALL_END,
   RUN_FINISHED outcome="interrupt"
   ```
   The permission args name the real call: `{"action":"execute","toolName":"gintrack_list_items","description":"execute gintrack_list_items with the following parameters: {\"project\":\"GIT\",\"type\":[\"story\"],\"status\":[\"done\"],…}","path":"<tmp>/repo"}`. **This is the event that never arrived before the fix** — previously the run hung after `TOOL_CALL_END` with no `TOOL_CALL_RESULT` and no `RUN_FINISHED`.

   Second leg: same `threadId=t1`, new `runId=r2`, same user message plus a trailing `{"id":"m2","role":"tool","toolCallId":"perm-dfc588fa-…","content":"{\"approved\": true}"}` (the shape `approvalFromMessage`, `internal/agui/hitl.go:145`, accepts). Events:
   ```
   STATE_SNAPSHOT, RUN_STARTED, TOOL_CALL_RESULT, STATE_DELTA,
   TEXT_MESSAGE_START, TEXT_MESSAGE_CONTENT×3 ("93" / " stories" / "."), STATE_DELTA, TEXT_MESSAGE_END,
   RUN_FINISHED outcome="success" result="93 stories."
   ```
   **93 is exactly right**: `grep -l '^status: done' docs/.pmngr/stories/*.md | wc -l` → 93 in the same snapshot.

5. **Chat turn with `AutoApprove = true` — PASS, no interrupt.** Same question, `threadId=t2`, after editing only the temp `.pando.toml` and restarting agui-serve:
   ```
   RUN_STARTED, STATE_SNAPSHOT, CUSTOM×2,
   TOOL_CALL_START toolCallName="gintrack_list_items", TOOL_CALL_ARGS, TOOL_CALL_END, STATE_DELTA,
   TOOL_CALL_RESULT, STATE_DELTA,
   TEXT_MESSAGE_START, TEXT_MESSAGE_CONTENT×3, STATE_DELTA, TEXT_MESSAGE_END,
   RUN_FINISHED outcome="success" result="93 stories."
   ```
   Zero `pando_permission_request` events. Both halves of the HITL policy (`internal/agui/hitl.go:49-62`) are therefore live and honoured.

6. **Negative check — pass, unchanged.** Asked to *"Run `ls` with the bash tool in the repository root and show me the output."* → zero tool calls of any name, `RUN_FINISHED outcome="success" result="I can't run \`ls\`: no Bash tool is available in this session."` The `[AGUI] Tools` allow-list still holds with the gateway off.

## What this closes

The three findings of the 2026-09-15 comment are resolved:

- **(b) now full, not half.** The tool keeps its real name `gintrack_list_items` (no `mcp_call_tool` proxy, no `tool_search`) *and* the run completes. The `GetMcpTools` package-cache bug that bound every MCP tool to `app.Permissions` instead of the AG-UI runtime's own service is gone.
- **(c) now pass.** `AutoApprove = false` produces a real `pando_permission_request` interrupt that a client can answer, and `AutoApprove = true` executes straight through. The "interim boundary is HITL" claim in GIT-US-0069 and docs/20 **does** hold on this Pando version, for MCP tool calls included.
- The generated template needs **no** further change: `ToolDiscovery`/`MCPGateway` off plus the `gintrack_*` allow-list is the correct and now-working configuration. The `mcp_call_tool` workaround documented on 2026-09-15 — with its "the allow-list stops being the boundary" caveat — is no longer needed and should be removed from docs/20 §7.

**Minimum Pando version for a working agent chat: v0.715.7-0.20260916155247-38abda0138cd** (the PANDO-US-0031 fix). Anything older hangs at `TOOL_CALL_END`.

## Not covered here

The semantic leg (`search.pando.mcpUrl` against `pando mcp-server`) was again out of scope and remains unverified. Acceptance criterion 3's `CHANGELOG.md` entry is the coordinator's to apply.

Every process started was killed by pid; `ss -ltn` shows 18090 and 18317 free and the user's companion on :7317 untouched. The temp repository copy and the extracted v1.6.0 source were deleted; the SSE logs and the redacted generated `.pando.toml` were kept.
