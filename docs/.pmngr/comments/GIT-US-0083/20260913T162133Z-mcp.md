---
type: comment
item: GIT-US-0083
author: mcp
created: 2026-09-13T16:21:33Z
kind: comment
---

Closed: the last open task, GIT-T-0207, landed the transform rules in `docs/03-data-model.md` §14.5 as R-KB-1 … R-KB-5 — title only in `summary`, the `## Feedback` block never leaving the repository, front matter stripped up and rebuilt down, a wikilink degrading to text unless its target is published, an attachment addressed by file name — beside §14.4, which is the block R-KB-2 depends on.

Reviewers should know one correction against the code: a page with neither a title nor a leading H1 is **not** refused; `PageToArticle` returns an empty summary and a warning, so the documentation says that rather than "cannot be published". The `{color:…}` / `{width=…}` asymmetry with issue descriptions (passed through here, stripped there) is written down with its reason. `CHANGELOG.md` was left to the agent that owns it this wave.
