---
type: comment
item: GIT-US-0083
author: mcp
created: 2026-09-13T15:31:05Z
kind: comment
---

GIT-T-0202 is `done`: the transport half of the attachment criterion landed in `internal/youtrack/attachments.go` (`UploadArticleAttachment`, `SyncArticleAttachments`, plus the issue twins), covered by `internal/youtrack/attachments_upload_test.go` against `httptest` for upload, skip-by-name-and-size, and failure. The upload builds its request outside the JSON path, so `Content-Type` is `multipart/form-data` with its own boundary and never `application/json`.

That means the story's third acceptance criterion — "Local attachments are uploaded and their refs rewritten to the article's attachment filenames" — is satisfied end to end now: `PageToArticle` names what to upload, `SyncArticleAttachments(ctx, articleID, fsys, []FileRef{...})` sends it. `FileRef{Name, Path}` is field-for-field `mapping.AttachmentRef`, so the caller converts the slice and nothing else. Leaving the checkbox to whoever closes the story, since the body is not mine to edit.
