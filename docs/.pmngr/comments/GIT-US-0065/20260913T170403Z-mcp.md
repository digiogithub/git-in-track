---
type: comment
item: GIT-US-0065
author: mcp
created: 2026-09-13T17:04:03Z
kind: comment
---

GIT-T-0139 is done: the field map now maps **values** as well as field names. The settings card reads `valueMappableFields` and each bundle-backed field's values from `GET /api/v1/youtrack/fields`, renders one row per value against this project's own statuses, priorities and item types, proposes by normalized name match with `isResolved === true` as the single fallback — an absent flag proposes nothing — and writes the nested `{field, values}` shape. It also fixed a live bug: the companion parser still read every `fieldMap` entry as a string, so after the wire shape changed the whole table came back empty.

The story is **not** closed, for one criterion only: `docs/03-data-model.md` §6 still says `field_map` "does not translate the values" (line 761) and describes the flat `map[string]string`. That file is outside this agent's file list this wave. Whoever owns `docs/03` should update §6 to the nested shape and the three value-mappable keys, after which every criterion here is satisfied.
