---
type: comment
item: GIT-US-0093
author: mcp
created: 2026-09-13T17:04:14Z
kind: comment
---

Closed with GIT-T-0219. The last seam is wired: `KbSyncToolbar` reads the project's `kb_sync` through `features/settings/youtrack-queries.ts`, so under `on_write` the manual publish relabels to **Publish now** and the toolbar says that saving a page already queues one — the button no longer describes the setting's work as its own. It stays enabled on purpose (publishing now is still wanted after a failed automatic job), and an `on_write` project whose direction is `pull` keeps the manual wording, because such a save publishes nothing. `docs/05-web-app.md` §3.1 documents the toolbar, badge, folder confirmation, conflict notice and this relabelling.

A reviewer must know one thing: the `CHANGELOG.md` entry GIT-T-0219 also asked for is **not** written. That file belonged to another agent this wave and was outside this one's file list, so the third criterion on GIT-T-0219 is left unticked and the entry is still owed.
