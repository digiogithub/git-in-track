---
type: comment
item: GIT-T-0189
author: mcp
created: 2026-09-15T16:44:35Z
kind: comment
---

Closing review, all criteria verified: matching terms highlighted, a snippet with no match still renders -> web/src/features/workspace/WorkspaceSearch.tsx:26-72 (`queryTerms`, `splitSnippet` returning one plain part when nothing matches), test "highlights the query terms the snippet contains"; HTML shown as text -> :98-113, parts render as `<mark>`/`<span>` children, no `dangerouslySetInnerHTML` anywhere in the file, test "shows HTML inside a snippet as text" (a `<script>` in the snippet); clamped with a keyboard-reachable expand -> :95-127 (`SNIPPET_CLAMP = 180`, a real `<button type="button" aria-expanded>` with a visible focus ring), test "clamps a long snippet behind an expand control"; `npx vitest run src/features/workspace/WorkspaceSearch.test.tsx` passes (10 tests).
