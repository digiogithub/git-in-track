---
type: comment
item: GIT-T-0092
author: mcp
created: 2026-09-13T16:19:51Z
kind: comment
---

Partial, from the wave that landed GIT-T-0083 and GIT-T-0088. This task is left `todo` on purpose.

The `docs/08-mcp-server.md` §4 half of the story's criteria is **already satisfied**: the tool table lists `list_inbox` (read, `inbox.list`), `create_inbox_item` (write, `item.create`) and `triage_inbox_item` (write, `inbox.triage`) with their modes and core methods. Nothing was needed there.

The remaining halves — `GET /api/v1/inbox`, `POST /api/v1/items/{id}/triage` and the `inbox.changed` WS topic in `docs/07-cli-and-api.md`, the `gintrack inbox` command tree, and the `CHANGELOG.md` entry with the "a project created before this change has no triage status and therefore no inbox" note — are outside this agent's file ownership (`docs/07-cli-and-api.md` and `CHANGELOG.md` belong to another agent this wave). They still need doing.

One new vault method also wants documenting wherever the CoreApi method catalogue lives (`docs/07`): **`comment.update`**, added in this wave — see the comment on GIT-US-0068.
