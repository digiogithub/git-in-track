---
type: comment
item: GIT-T-0016
author: mcp
created: 2026-09-15T16:43:26Z
kind: comment
---

Closing review, all 3 criteria verified. Bearer-auth: `p.Route("/agent", s.mountAgent)` sits inside the `s.bearerAuth` group (internal/server/api.go:110, next to `/mcp`), 401 without a token -> `TestAgentRequiresCompanionToken` (agent_test.go:471). Timeout exemption: `timeoutExceptStream` passes through anything under `agentPath+"/"` alongside `/events`, `/mcp` and the CORS proxy (internal/server/server.go:462-466), asserted by driving the middleware in `TestAgentRoutesAreExemptFromTheRequestTimeout` (agent_test.go:652) — a wall-clock test would have been useless. No Pando URL / feature off -> 501 `not_implemented` from `agentUpstream` (agent.go:374-393), `TestAgentDisabledReportsNotImplemented` (:489) covers both halves. `agentState` is constructed in `New` (server.go:299). `go test -race ./internal/server/...` green.
