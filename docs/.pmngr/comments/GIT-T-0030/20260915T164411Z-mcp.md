---
type: comment
item: GIT-T-0030
author: mcp
created: 2026-09-15T16:44:11Z
kind: comment
---

Closing review, all 3 criteria verified. The seam is declared on `DataProvider` (web/src/api/provider.ts, eight agent methods plus `Capabilities.agent` at :1237 and the new `not_supported` error code) and implemented in every provider: companion-provider.ts (POST `/api/v1/agent/run?repo=`, `Accept: text/event-stream`, SSE decoded by the SDK's `parseSSE`, aborted fetch re-thrown untouched), browser-provider.ts:1647+ (typed `ProviderError` with `not_supported` for all eight, capability false at :334, streams refusing on first pull), fake-provider.ts (`FakeAgent` script at :900/:1356 with `agentRuns`/`agentCancels`/`agentDeletes`). `npx tsc --noEmit` clean and `npm run build` green, so everything compiles. Typed rejection -> browser-provider.test.ts:781. Scripted replay -> src/api/agent-provider.test.ts (8 tests) and the whole of store.test.ts driving FakeProvider; companion-provider.test.ts adds a real `ReadableStream` SSE body split mid-frame. Wider seam than the task text (eight routes, not two) is the recorded 2026-09-15 decision, taken so the interface is not changed twice.
