---
type: comment
item: GIT-T-0038
author: mcp
created: 2026-09-15T16:44:22Z
kind: comment
---

Closing review, all 4 criteria verified. The store is web/src/features/agent/store.ts (named `store.ts` rather than `agent-store.ts`; the folder already namespaces it — recorded 2026-09-15). Stable thread id with a fresh run id per send, whole transcript resent -> store.test.ts:62 and :75; persistence across a reload -> :90 (`localStorage` per repo, every access try/caught). Cancel aborts the controller and also calls `cancelAgentRun`, settling without an open message -> store.test.ts:243. Thread switching swaps messages and the state document -> :263, a new thread starts empty -> :281. Error propagation -> :207 (`RUN_ERROR`), :218 (mid-stream transport failure), :230 (refusal to start). `runStatus` is decided in `drive()`, not in the reducer, so the store holds conversation state only, per the app/store.ts rule. `ThreadGuard` over `BroadcastChannel` covered at threads.test.ts:80-123 and store.test.ts:336/:360. 20+ tests, Vitest green.
