---
type: comment
item: GIT-US-0094
author: mcp
created: 2026-09-13T15:42:25Z
kind: comment
---

MCP half landed: `publish_kb_page_to_youtrack` and `sync_kb_page_from_youtrack` in `internal/mcp/tools_youtrack.go`, both write tools, both queueing a job and returning `{jobId, pages:[{path, action, …}]}`. GIT-T-0220 is `done`, GIT-T-0221 `in_review` with one criterion unticked; GIT-T-0222 (CLI) and GIT-T-0223 (docs/07 + CHANGELOG) are left `todo` with the call contracts in their comments, because `cmd/gintrack`, `docs/07-cli-and-api.md` and `CHANGELOG.md` belong to other agents this wave.

Reviewer must know: every `path` goes through `s.guard.Check` **before** the core is asked anything, and `TestKBSyncToolsGuardThePath` asserts both the `forbidden_path` refusal and that nothing was queued. `articleId`, `url` and `error` on a page entry are filled by the job's own report, not by the call that queued it — `action` and `path` are all the queueing call can honestly say. `docs/08-mcp-server.md` §4.18 documents both tools together with the three rules that govern the boundary in either direction (the title lives only in the summary; the `## Feedback` block never leaves the repository; a both-sides-changed page produces `<page>.conflict.md` and nothing is merged).

The CLI story's "a conflict must not look like success" requirement is unimplemented and is called out in GIT-T-0222's comment: `youtrack.kb.status` is what the command reads to decide the exit code.
