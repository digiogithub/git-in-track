---
type: comment
item: GIT-T-0130
author: mcp
created: 2026-09-15T16:44:48Z
kind: comment
---

Closing review, all four criteria verified: the listener answers while the export runs -> internal/server/server.go:377 calls startCorpusSync, which runs `searchState.exportAll` in a goroutine (internal/server/pandosync_hub.go:22-28, :31-56) + TestCorpusExportsOnStartWithoutBlockingTheListener (real socket, GET /api/v1/health answered during the export, and an unchanged second export keeps the mtime); unchanged second export writes nothing -> internal/pandosync/exporter.go:474-492 (content-hash skip, falling back to the bytes on disk) + TestExportAllSkipsUnchangedDocuments, TestExportAllSkipsFromDiskAfterRestart; prune of vanished sources -> exporter.go:533-586 + TestExportAllPrunesVanishedSourcesAndEmptyDirectories; shutdown cancels cleanly -> exporter.go:278-280, :557 and pandosync_hub.go:39-43 + TestExportAllIsCancellable; 10 000-item timing -> TestExportAllLargeCorpusStaysWithinBudget (export + unchanged re-export, both far inside the bound). Progress reaches the hub as `search.progress` shaped like `sync.progress` (search_settings.go:229-256). `go test -race ./internal/server/... ./internal/pandosync/` green. Note: docs/02 §9 has no corpus-export row, so the 20 s bound in the test is the exporter's own, not the table's.
