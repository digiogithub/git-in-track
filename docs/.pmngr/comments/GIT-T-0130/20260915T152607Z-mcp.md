---
type: comment
item: GIT-T-0130
author: mcp
created: 2026-09-15T15:26:07Z
kind: comment
---

Partially landed; staying **in_progress** because the server half cannot be written in this wave (`internal/server` is owned by wave 2).

Done in `internal/pandosync/exporter.go`:
- `Exporter.ExportAll(ctx) (Stats, error)` walks every item and page from `Source`, serialises each and writes only those whose content hash differs (from the in-memory cache, or from the bytes on disk after a restart) — `TestExportAllSkipsUnchangedDocuments`, `TestExportAllSkipsFromDiskAfterRestart`.
- Corpus files with no surviving source are pruned, emptied directories removed — `TestExportAllPrunesVanishedSourcesAndEmptyDirectories`.
- Cancellable: `ExportAll` checks `ctx.Err()` per document and in the prune walk, returning `context.Canceled` and leaving every file it did write complete — `TestExportAllIsCancellable`.
- `Options.OnProgress func(Progress{Done, Total})` is the seam for publishing `sync.progress`-shaped events without importing `internal/server`.
- `Exporter.LastExport() Stats` for the settings card.
- 10 000-item timing check: `TestExportAllLargeCorpusStaysWithinBudget` exports 10 000 items and then re-exports them unchanged, asserting both stay well inside the §9 budget (bound set at 20 s over `core.MemFS`; measured far below).

Remaining for wave 2: start `ExportAll` in a goroutine from `Server.Start` with the shutdown context, and publish `Progress` on the hub. The "server accepts requests immediately" criterion can only be tested there.
