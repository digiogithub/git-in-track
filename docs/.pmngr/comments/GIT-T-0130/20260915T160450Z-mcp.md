---
type: comment
item: GIT-T-0130
author: mcp
created: 2026-09-15T16:04:50Z
kind: comment
---

Finished in wave 2. The server half now exists.

- `internal/server/pandosync_hub.go`: `(*Server).startCorpusSync(ctx)` is called from `Server.Start` right after `startWatch`, and runs `searchState.exportAll` in a goroutine with the shutdown context. One exporter per ready mount, rooted at `<corpusDir>/<repo id>` (`search.pando.corpusDir`, else `Options.SearchCorpusDir` = `<index.cacheDir>/pando-kb`) — the same path `gintrack agent init` writes into `KBPath`.
- Progress reaches the hub as `search.progress`, shaped like `sync.progress`, under the operation id `startup` (`searchState.progressFor` → `Options.OnProgress`).
- A cancelled export is logged at debug and not reported as a failure.

Tests (`internal/server/pandosync_hub_test.go`): `TestCorpusExportsOnStartWithoutBlockingTheListener` starts a real listener on a free port, proves `GET /api/v1/health` answers **over a socket** while the export runs, waits for the corpus files, then asserts a second `ExportAll` writes nothing and does not touch the mtime of an unchanged document (the property Pando's mtime skip depends on). The 10 000-item timing check stays where it was, in `internal/pandosync`.

Reviewer: `internal/server/pandosync_hub.go` and the `startCorpusSync` line in `Server.Start`.
