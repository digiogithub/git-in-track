---
type: comment
item: GIT-T-0163
author: mcp
created: 2026-09-15T16:43:20Z
kind: comment
---

Closing review, all criteria verified: corpus paths resolve for both kinds -> internal/server/search_pando.go:214-252 (`parseCorpusPath`, scanned from the right) + `resolve` at :166-206 re-reading title/path/project from `vault.Item` / `vault.PageByProjectPath`, test `TestParseCorpusPath` (7 cases); unresolvable and deleted candidates dropped and counted -> search_pando.go:105-127 (`dropped` counter + debug line), :188-190 (`it.Deleted`); score, snippet and origin on every hit -> search_pando.go:176-181 (`Score`, `chunkSnippet`, `Source: core.SearchSourcePando`); tests -> `go test -race ./internal/server/` `TestParseCorpusPath`, `TestSearchMergesSemanticHitsAfterExactOnes`, `TestSearchOverFetchesAndScopesByPathPrefix` (empty-result case) all pass. Native-only: the file lives in internal/server and nothing moved into internal/core (WASM build still green).
