---
type: comment
item: GIT-US-0061
author: mcp
created: 2026-09-13T21:15:47Z
kind: comment
---

Aligned with the 2026-09-13 restricted-agent decision.

Changed:
- Opens by stating that HITL plus `AutoApprove = false` is the interim security boundary, not a convenience: until the adapter-wide tool allow-list exists the agent is Pando's full coder agent and the dialog is the only thing in the way. Added a criterion that the docs and ADR say this plainly.
- Answer shapes for permissions and questions now come from `@pando-ai/sdk/agui`'s HITL helpers instead of hand-built trailing `tool` payloads.
- Added the container / low-privilege-user second layer as a Note.
- Dropped the `examples/copilotkit/app/page.tsx` reference (CopilotKit is out); the `hitl.go:117` argument shape reference is kept.

Depends on: PANDO-EP-0002 (`[AGUI] Tools` allow-list + `Mesnada = false`) — demotes HITL from sole boundary to defence in depth. PANDO-EP-0001 — the SDK HITL helpers.
