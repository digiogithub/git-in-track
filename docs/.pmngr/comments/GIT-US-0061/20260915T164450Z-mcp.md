---
type: comment
item: GIT-US-0061
author: mcp
created: 2026-09-15T16:44:50Z
kind: comment
---

Closing-pass verification (read-only review of HEAD 62f03ec). All eight criteria met and all four children (GIT-T-0067/0070/0074/0077) are `done`.

Evidence, criterion by criterion: interrupt names the tool and shows arguments verbatim as text -> `ui/PermissionDialog.tsx:97-131`; approve/deny/always built with the SDK's HITL helpers, never a local payload -> `hitl.ts:22-30` importing `approve`/`deny`/`answerQuestion`/`cancelQuestion` from `@pando-ai/sdk/agui/client`, wired at `ui/InterruptSlot.tsx:105-124` and `store.ts:318`; dismissal is an explicit denial, not a parked run -> `PermissionDialog.tsx:70-73,80-84,139-158` (confirm-then-deny), test `PermissionDialog.test.tsx:66,78`; approve-always is per thread, in memory, forgotten on reload -> `store.ts:486-490` + `settle()` `:313-319`, cleared at `:362,384,405`, test `store.tools.test.ts:220`; question answer/cancel -> `ui/QuestionDialog.tsx` + `hitl.ts:186-208`, tests `QuestionDialog.test.tsx:44,68,83`; state panel renders todos/sub-agents/tokens/files live and persists across turns -> `ui/StatePanel.tsx:132-196`, tests `StatePanel.test.tsx:29,43,59`; docs and ADR state the interim boundary plainly and that the allow-list supersedes rather than replaces it -> `docs/adr/ADR-035-agent-interface-over-ag-ui.md:109-116` and `docs/05-web-app.md` §19.7; vitest covers approve, deny, dismiss-as-deny, answer, cancel and delta application -> the test names above plus `store.tools.test.ts:209,248,261`.

Re-run here: `npx vitest run src/features/agent src/app/router.test.ts src/app/layout/AppShell.test.tsx` — 17 files / 162 tests green; `node web/scripts/check-design-tokens.mjs` — 62 assertions pass.
