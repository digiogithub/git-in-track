---
item: GIT-US-0030
author: claude
created: 2026-09-04T15:00:00Z
type: comment
---

Branch: `feat/phase-6-retros-metrics`. 1.0 is **prepared, not released** — no tag was
created, nothing was pushed or published, and no workflow was triggered. Cutting `v1.0.0`
stays with the maintainer.

Landed: `docs/12-release-readiness-1-0.md` (evidence for every milestone exit criterion and
vision goal, the consolidated gap list, and the maintainer's remaining steps) and
`CHANGELOG.md` (the 1.0 notes, the compatibility promise, the known limitations and the
operational notes). `README.md`, `AGENTS.md`, `docs/01`, `docs/07` and `docs/09` were
corrected where they still described a plan or a state that never happened —
`docs/09` said `schemaVersion` where the field is `schema`, and `docs/07` §2.3 still
promised a Homebrew *formula*, the wrong Scoop repository and "no Docker image for 1.0".

`docs/11-roadmap.md` was left untouched: `AGENTS.md` marks it a human-only area until 1.0.

Five acceptance criteria cannot be satisfied without tagging or without a macOS/Windows
host, and one (`gintrack migrate`) needs a command that does not exist. They are left
unticked and the reasons are in the story Notes. Two criteria were ticked: the frozen
`schema: 1` with its written promise, and the user documentation.

Gates on 2026-09-04, Linux/amd64, commit `c524914`: `make lint` 0 issues; `make test` green
(515 Go tests with `-race`, Vitest 54 files / 476 tests); `make wasm-smoke` 32/32;
`make build`; `goreleaser check` clean; a `--snapshot --skip=publish` run produced all six
archives, `checksums.txt`, the cask and the Scoop manifest, and every checksum verified.
The arm64 container image is the one thing that failed locally, for want of binfmt
emulation — the release runner installs it via `docker/setup-qemu-action@v3`.

One honest miss worth a decision before the tag: `internal/core` coverage is 83.6 %, and
milestone 1's exit criterion is ≥ 85 %.
