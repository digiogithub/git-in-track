---
type: comment
item: GIT-T-0111
author: mcp
created: 2026-09-13T15:51:40Z
kind: comment
---

Left `todo` on purpose. Every acceptance criterion here is a change under `docs/` (`docs/05-web-app.md`, `docs/02-architecture.md`, `CHANGELOG.md`), and `docs/` belongs to another agent this wave — the web agent was scoped to `web/` only and must not edit it.

The dialog itself is implemented and merged (GIT-T-0095, 0099, 0104 and 0108 are `done`), so the material for the documentation is all in place: the entry lives in the backlog toolbar (`ItemTable` header, `ImportFromYouTrackButton`), gated on `youtrackSupported && youtrack`; the options are subtask depth 0–5, linked issues, comments, attachments, plus a disabled "land in Inbox" pointing at the Inbox epic; preview calls the same operation with the same options as run and writes nothing; and run either enqueues a `youtrack.import` job followed over `sync.job.*` or answers the finished result inline, ending in a per-issue summary with links and error text.
