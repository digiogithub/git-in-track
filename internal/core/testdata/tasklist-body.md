## Description

A body that exercises every shape the toggle has to leave alone.

## Acceptance Criteria

- [x] Creating an epic, story, task or milestone produces a valid file.
- [ ] The status picker offers only transitions allowed by the workflow.
  - [ ] A nested criterion keeps its indentation.
* [X] A star bullet is a task-list item too.
1. [ ] So is an ordered marker.

Not a task: - [ ] this one is inline prose, not a list item.

```markdown
- [ ] A checkbox inside a fence is code, never a checkbox.
```

| Column | Value |
| ------ | ----- |
| trailing spaces below | kept |

## Notes

A paragraph with a trailing double space  
that must survive a toggle untouched.
