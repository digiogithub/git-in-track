import { describe, it, expect } from 'vitest'

// Implements: WEB/WEB-SP-0001.R1
export function parse(input: string): string {
  // Implements: WEB-SP-0001.R2
  return input
}

/**
 * Implements: WEB-SP-0001.R3
 */
export const render = (x: number) => {
  return x
}

export class Board {
  // Implements: WEB-SP-0001.R4
  move(id: string): void {
    const s = "/* not a comment"
    // Implements: WEB-SP-0001.R5
  }
}

describe('parser', () => {
  // Verifies: WEB-SP-0001.R1,
  it('reads a heading', () => {
    expect(parse('x')).toBe('x')
  })

  it('reads a list', () => {
    // Verifies: WEB-SP-0001.R2 */
    expect(1).toBe(1)
  })
})

const s = `
// Implements: WEB-SP-0001.R9
`
